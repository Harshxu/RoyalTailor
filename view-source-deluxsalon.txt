
<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="google" content="notranslate">

  <!-- ============================== CACHE CONTROL META TAGS ============================== -->
  <!-- Forces browsers & ISP proxies to revalidate and serve the newest index.html -->
  <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate, max-age=0">
  <meta http-equiv="Pragma" content="no-cache">
  <meta http-equiv="Expires" content="0">

  <!-- Client-side Cache Invalidation & Force Reload Handler -->
  <script>
    (function () {
      // Incrementing APP_BUILD_VERSION forces any cached browser to do a clean reload once
      var CURRENT_APP_VERSION = "2026.08.16.v3";
      try {
        var cachedVersion = localStorage.getItem('deluxe_client_app_version');
        if (cachedVersion && cachedVersion !== CURRENT_APP_VERSION) {
          localStorage.setItem('deluxe_client_app_version', CURRENT_APP_VERSION);
          // Force hard reload bypassing browser cache
          if (window.location.search.indexOf('v=') === -1) {
            window.location.replace(window.location.pathname + '?v=' + Date.now());
          }
        } else if (!cachedVersion) {
          localStorage.setItem('deluxe_client_app_version', CURRENT_APP_VERSION);
        }
      } catch (e) {}
    })();
  </script>

  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-HLPSPY313T"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    gtag('config', 'G-HLPSPY313T');
  </script>

  <!-- ============================== PRIMARY SEO META TAGS ============================== -->
  <title>Deluxe Salon | Delux Salon 2000s Indian Retro Ambient Radio – deluxsalon.in</title>
  <meta name="title" content="Deluxe Saloon | Delux Salon 2000s Indian Retro Ambient Radio">
  <meta name="description" content="Deluxe Saloon (डीलक्स सैलून) is a free 2000s Indian retro ambient saloon radio. Stream nostalgic Bollywood barbershop music, tune the vintage radio and relive the classic Deluxe Salon vibe, only at deluxsalon.in.">
  <meta name="keywords" content="Deluxe Saloon, Deluxe Salon, delux salon, delux saloon website, deluxe salon website, deluxe salon 90s music, deluxe saloon 2000s music, deluxe salon songs, deluxe salon playlist, Indian retro saloon radio, ambient saloon radio, 2000s Bollywood radio, barbershop nostalgia radio, deluxsalon.in">
  <meta name="robots" content="index, follow, max-image-preview:large">
  <meta name="author" content="Deluxe Saloon">
  <meta name="theme-color" content="#120806">
  <link rel="canonical" href="https://deluxsalon.in/">
  <link rel="icon" type="image/png" href="/img/favicon.png">
  <link rel="apple-touch-icon" href="/img/favicon.png">
    <!-- ============================== PWA: manifest + icons + iOS meta ============================== -->
  <link rel="manifest" href="/manifest.json">
  <link rel="apple-touch-icon" href="/img/icon-192.png">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <meta name="apple-mobile-web-app-title" content="Delux Salon">

  <!-- Hreflang (single locale, kept explicit for search engines) -->
  <link rel="alternate" href="https://deluxsalon.in/" hreflang="en-in">
  <link rel="alternate" href="https://deluxsalon.in/" hreflang="hi-in">
  <link rel="alternate" href="https://deluxsalon.in/" hreflang="x-default">

  <!-- ============================== OPEN GRAPH / SOCIAL ============================== -->
  <meta property="og:type" content="website">
  <meta property="og:url" content="https://deluxsalon.in/">
  <meta property="og:site_name" content="Deluxe Saloon">
  <meta property="og:locale" content="en_IN">
  <meta property="og:title" content="Deluxe Saloon — 2000s Indian Retro Ambient Saloon Radio">
  <meta property="og:description" content="Free ambient radio inspired by the golden era of the Indian neighbourhood saloon. Tune in for nonstop 2000s Bollywood barbershop nostalgia.">
  <meta property="og:image" content="https://deluxsalon.in/img/og-cover.jpg">
  <meta property="og:image:width" content="1200">
  <meta property="og:image:height" content="630">

  <meta name="twitter:card" content="summary_large_image">
  <meta name="twitter:title" content="Deluxe Saloon — 2000s Indian Retro Ambient Saloon Radio">
  <meta name="twitter:description" content="Free ambient radio inspired by the golden era of the Indian neighbourhood saloon — nonstop 2000s Bollywood barbershop nostalgia.">
  <meta name="twitter:image" content="https://deluxsalon.in/img/og-cover.jpg">

  <!-- Tailwind CSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          screens: { 'xs': '400px' },
          colors: {
            amber: { 350: '#f5a524' },
          }
        }
      }
    }
  </script>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Rozha+One&family=Yatra+One&family=Space+Grotesk:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
    html { background:#120806; }
    body { font-family: 'Space Grotesk', sans-serif; }
    .font-hindi { font-family: 'Yatra One', 'Rozha One', serif; }
    .font-display { font-family: 'Rozha One', serif; }

    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after { animation-duration: 0.01ms !important; animation-iteration-count: 1 !important; transition-duration: 0.01ms !important; scroll-behavior: auto !important; }
    }

    /* Unified Glassy Header Controls (Exact Equal Dimensions & Proportions on Mobile & Desktop) */
    .header-pill {
      height: 36px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      background: rgba(18, 8, 6, 0.65);
      border: 1px solid rgba(255, 255, 255, 0.22);
      border-radius: 9999px;
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4), inset 0 1px 1px rgba(255, 255, 255, 0.15);
      color: rgba(255, 255, 255, 0.92);
      font-size: 11.5px;
      font-weight: 600;
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      box-sizing: border-box;
      white-space: nowrap;
    }
    .header-pill:hover {
      background: rgba(255, 255, 255, 0.18);
      border-color: rgba(255, 255, 255, 0.35);
      color: #ffffff;
      transform: translateY(-1px);
    }
    .header-pill:active {
      transform: scale(0.97);
    }

    /* Theme dropdown styling (matched to header-pill) */
    .theme-select {
      appearance: none;
      -webkit-appearance: none;
      -moz-appearance: none;
      height: 36px;
      line-height: 34px;
      background: rgba(18, 8, 6, 0.65);
      border: 1px solid rgba(255, 255, 255, 0.22);
      color: rgba(255, 255, 255, 0.92);
      font-size: 11.5px;
      font-weight: 600;
      padding: 0 12px;
      border-radius: 9999px;
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      cursor: pointer;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.4), inset 0 1px 1px rgba(255, 255, 255, 0.15);
      transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
      box-sizing: border-box;
      text-align: center;
    }
    .theme-select:hover {
      border-color: rgba(245, 158, 11, 0.6);
      background: rgba(255, 255, 255, 0.18);
      color: #ffffff;
      transform: translateY(-1px);
    }
    .theme-select option {
      background: #1c0704;
      color: #fff;
    }
    .theme-select-wrap {
      position: relative;
      display: inline-block;
    }
    .theme-select-arrow {
      position: absolute;
      right: 8px;
      top: 50%;
      transform: translateY(-50%);
      width: 12px;
      height: 12px;
      color: #f59e0b;
      pointer-events: none;
    }

    .music-player {
      display: flex;
      align-items: center;
      gap: 12px;
      width: 100%;
      max-width: 580px;
      background: rgba(20, 10, 8, 0.65);
      border: 1px solid rgba(255, 255, 255, 0.22);
      border-radius: 24px;
      padding: 12px 18px;
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.9), inset 0 1px 1px rgba(255, 255, 255, 0.2);
      backdrop-filter: blur(24px);
      -webkit-backdrop-filter: blur(24px);
    }
    /* Circular, spinning "cassette/vinyl" cover art */
    .music-cover-frame {
      width: 48px; height: 48px; border-radius: 50%; position: relative; flex-shrink: 0; cursor: pointer;
      border: 1px solid rgba(255, 255, 255, 0.25); background: #1c1917; padding: 0; overflow: visible;
      transition: transform 0.2s, border-color 0.2s;
    }
    .music-cover-frame:hover { transform: scale(1.05); border-color: #f59e0b; }
    .music-cover {
      width: 100%; height: 100%; border-radius: 50%; background-color: #1c1917; background-size: cover; background-position: center;
      overflow: hidden;
    }
    .music-cover.spinning { animation: discSpin 3.2s linear infinite; }
    @keyframes discSpin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
    .music-cover-hole {
      position: absolute; top: 50%; left: 50%; width: 7px; height: 7px; margin: -3.5px 0 0 -3.5px;
      background: #0a0a0a; border-radius: 50%; border: 1px solid rgba(255,255,255,.5); pointer-events: none;
    }
    .track-block { flex: 1; min-width: 0; display: flex; flex-direction: column; gap: 2px; }
    .track-top { display: flex; align-items: center; justify-content: space-between; gap: 8px; }
    .track-name {
      font-size: 13px; font-weight: 700; color: #ffffff;
      white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
    }
    .station { font-size: 10px; color: rgba(255, 255, 255, 0.6); margin: 0; }
    .progress-row {
      display: flex; align-items: center; gap: 8px; margin-top: 4px;
      font-size: 10px; font-family: monospace; color: rgba(255, 255, 255, 0.6);
    }
    .progress {
      flex: 1; height: 5px; background: rgba(255, 255, 255, 0.2);
      border-radius: 999px; overflow: hidden; cursor: pointer; position: relative;
    }
    .progress-fill { height: 100%; background: #f59e0b; width: 0%; border-radius: 999px; transition: width 0.2s linear; }
    .player-actions { display: flex; align-items: center; gap: 6px; flex-shrink: 0; }
    .icon-button {
      background: rgba(255, 255, 255, 0.1); border: 1px solid rgba(255, 255, 255, 0.2);
      color: rgba(255, 255, 255, 0.9); width: 32px; height: 32px; border-radius: 50%;
      cursor: pointer; display: flex; align-items: center; justify-content: center;
      transition: all 0.2s; backdrop-filter: blur(8px);
    }
    .icon-button:hover { color: #ffffff; background: rgba(255, 255, 255, 0.25); transform: scale(1.08); }
    .icon-button:focus-visible, .music-cover-frame:focus-visible, .theme-select:focus-visible {
      outline: 2px solid #fbbf24; outline-offset: 2px;
    }
    .icon-button svg { width: 16px; height: 16px; fill: currentColor; }
    .play-button {
      background: #f59e0b; color: #000000; width: 40px; height: 40px; border-radius: 50%;
      border: 1px solid rgba(255, 255, 255, 0.4); box-shadow: 0 0 20px rgba(245, 158, 11, 0.5);
    }
    .play-button:hover { background: #fbbf24; color: #000000; transform: scale(1.1); }
    .play-button svg { width: 18px; height: 18px; fill: currentColor; }

    /* Live Chat button badge + panel */
    .tweak-pill {
      height: 32px;
      min-height: 32px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      background: rgba(18, 8, 6, 0.35);
      border: 1px solid rgba(255, 255, 255, 0.22);
      border-radius: 9999px;
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.45);
      color: rgba(255, 255, 255, 0.9);
      font-size: 11.5px;
      font-weight: 600;
      padding: 0 14px;
      white-space: nowrap;
      transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
      cursor: pointer;
      user-select: none;
      position: relative;
      touch-action: manipulation;
      -webkit-tap-highlight-color: transparent;
    }
    .tweak-pill:hover {
      background: rgba(245, 158, 11, 0.25);
      border-color: rgba(245, 158, 11, 0.6);
      color: #fde68a;
      transform: translateY(-1px) scale(1.03);
    }
    .tweak-pill:active {
      transform: translateY(1px) scale(0.96);
    }
    .live-chat-badge {
      position: absolute;
      top: -6px;
      right: -6px;
      min-width: 16px;
      height: 16px;
      padding: 0 4px;
      border-radius: 9999px;
      background: #ef4444;
      color: #fff;
      font-size: 10px;
      font-weight: 800;
      display: none;
      align-items: center;
      justify-content: center;
      line-height: 1;
      box-shadow: 0 0 0 2px #120806;
    }
    .live-chat-badge.show { display: flex; }

    #liveChatPanel {
      position: fixed;
      inset: 0;
      z-index: 70;
      display: none;
      align-items: flex-end;
      justify-content: center;
      background: rgba(0, 0, 0, 0.6);
      backdrop-filter: blur(4px);
      padding: 0;
    }
    #liveChatPanel.open { display: flex; }
    @media (min-width: 640px) {
      #liveChatPanel { align-items: center; padding: 16px; }
    }
    .chat-card {
      width: 100%;
      max-width: 440px;
      max-height: 78vh;
      background: radial-gradient(circle at top, #3a1a0c 0%, #0d0503 100%);
      border: 1px solid rgba(245, 158, 11, 0.3);
      border-radius: 20px 20px 0 0;
      box-shadow: 0 -10px 40px rgba(0, 0, 0, 0.7);
      display: flex;
      flex-direction: column;
      overflow: hidden;
    }
    @media (min-width: 640px) {
      .chat-card { border-radius: 20px; }
    }
    .chat-messages {
      flex: 1;
      overflow-y: auto;
      padding: 14px;
      display: flex;
      flex-direction: column;
      gap: 6px;
      min-height: 260px;
    }
    .chat-msg-row {
      display: flex;
      width: 100%;
      animation: chatMsgIn 0.2s ease-out;
    }
    .chat-msg-row.own { justify-content: flex-end; }
    .chat-msg-row.other { justify-content: flex-start; }
    @keyframes chatMsgIn {
      from { opacity: 0; transform: translateY(6px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .chat-msg {
      max-width: 80%;
      border-radius: 14px;
      padding: 6px 10px;
      font-size: 12.5px;
      line-height: 1.4;
      word-break: break-word;
    }
    .chat-msg-row.other .chat-msg {
      background: rgba(255, 255, 255, 0.07);
      border: 1px solid rgba(255, 255, 255, 0.09);
      border-bottom-left-radius: 3px;
    }
    .chat-msg-row.own .chat-msg {
      background: rgba(245, 158, 11, 0.22);
      border: 1px solid rgba(245, 158, 11, 0.4);
      border-bottom-right-radius: 3px;
    }
    .chat-msg .chat-msg-name {
      font-weight: 700;
      font-size: 11px;
      display: block;
      margin-bottom: 1px;
    }
    .chat-msg .chat-msg-time {
      display: block;
      font-size: 9px;
      color: rgba(255, 255, 255, 0.35);
      margin-top: 2px;
      text-align: right;
    }

    .scroll-cue {
      animation: bob 2.2s ease-in-out infinite;
    }
    @keyframes bob {
      0%, 100% { transform: translateY(0); opacity: .55; }
      50% { transform: translateY(6px); opacity: 1; }
    }

    .faq-item[open] summary .faq-chevron { transform: rotate(180deg); }

    /* Rain & Lightning Atmospheric Overlay */
    #rainCanvas {
      position: fixed;
      inset: 0;
      width: 100vw;
      height: 100vh;
      pointer-events: none;
      z-index: 15;
      opacity: 0;
      transition: opacity 0.8s ease-in-out;
    }
    #rainCanvas.active {
      opacity: 1;
    }
    #lightningFlashOverlay {
      position: fixed;
      inset: 0;
      width: 100vw;
      height: 100vh;
      pointer-events: none;
      z-index: 18;
      background: rgba(230, 242, 255, 0.92);
      opacity: 0;
      mix-blend-mode: screen;
      transition: opacity 0.05s ease-out;
    }
    .rain-toggle-pill {
      height: 32px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      background: rgba(18, 8, 6, 0.35);
      border: 1px solid rgba(255, 255, 255, 0.22);
      border-radius: 9999px;
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.45);
      color: rgba(255, 255, 255, 0.9);
      font-size: 11.5px;
      font-weight: 600;
      padding: 0 14px;
      transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
      cursor: pointer;
    }
    .rain-toggle-pill:hover {
      background: rgba(30, 58, 138, 0.4);
      border-color: rgba(96, 165, 250, 0.6);
      color: #93c5fd;
      transform: scale(1.04);
    }
    .rain-toggle-pill.active {
      background: rgba(30, 58, 138, 0.65);
      border-color: rgba(96, 165, 250, 0.85);
      color: #bfdbfe;
      box-shadow: 0 0 20px rgba(59, 130, 246, 0.45), inset 0 1px 2px rgba(255, 255, 255, 0.25);
    }

    .share-toggle-pill {
      height: 32px;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      gap: 6px;
      background: rgba(18, 8, 6, 0.35);
      border: 1px solid rgba(255, 255, 255, 0.22);
      border-radius: 9999px;
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.45);
      color: rgba(255, 255, 255, 0.9);
      font-size: 11.5px;
      font-weight: 600;
      padding: 0 14px;
      transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
      cursor: pointer;
    }
    .share-toggle-pill:hover {
      background: rgba(37, 211, 102, 0.18);
      border-color: rgba(37, 211, 102, 0.55);
      color: #6ee7a7;
      transform: scale(1.04);
    }

    /* Hide scrollbar for the single-row pill strip (Baarish / Feedback / Share) */
    .no-scrollbar {
      -ms-overflow-style: none;
      scrollbar-width: none;
    }
    .no-scrollbar::-webkit-scrollbar {
      display: none;
    }

    /* Universal Modal Styles */
    .app-modal {
      display: none;
      align-items: center;
      justify-content: center;
      position: fixed;
      inset: 0;
      z-index: 60;
      padding: 16px;
      background: rgba(0, 0, 0, 0.82);
      backdrop-filter: blur(8px);
      -webkit-backdrop-filter: blur(8px);
      animation: modalFadeIn 0.25s ease-out forwards;
    }
    .app-modal.open { display: flex; }
    @keyframes modalFadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }
    .app-modal-card {
      position: relative;
      width: 100%;
      max-width: 540px;
      background: #1c0704;
      background: radial-gradient(circle at top, #2b0b06 0%, #150503 100%);
      border: 1px solid rgba(245, 158, 11, 0.35);
      border-radius: 24px;
      box-shadow: 0 25px 60px rgba(0, 0, 0, 0.85), inset 0 1px 1px rgba(255, 255, 255, 0.15);
      padding: 24px sm:32px;
      text-align: center;
      max-height: 90vh;
      overflow-y: auto;
    }
  </style>

  <!-- ============================== SCHEMA.ORG STRUCTURED DATA ============================== -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "Deluxe Saloon",
    "alternateName": ["Deluxe Salon", "Delux Salon", "Delux Saloon", "डीलक्स सैलून"],
    "url": "https://deluxsalon.in/",
    "inLanguage": ["en-IN", "hi-IN"],
    "description": "Deluxe Saloon is a free 2000s Indian retro ambient saloon radio experience with classic Bollywood barbershop tunes.",
    "potentialAction": {
      "@type": "ListenAction",
      "target": "https://deluxsalon.in/"
    }
  }
  </script>
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "FAQPage",
    "mainEntity": [
      {
        "@type": "Question",
        "name": "What is Deluxe Saloon?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Deluxe Saloon (also written Deluxe Salon or Delux Salon) is a free ambient radio website at deluxsalon.in that recreates the sound and mood of a classic 2000s Indian neighbourhood barbershop, complete with nostalgic Bollywood tunes and barber chatter."
        }
      },
      {
        "@type": "Question",
        "name": "Is Deluxe Saloon free to use?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Yes. Deluxe Saloon streams entirely for free in your browser, with no sign-up required, on both desktop and mobile."
        }
      },
      {
        "@type": "Question",
        "name": "What kind of music plays on Deluxe Saloon?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Deluxe Saloon plays a curated 2000s Indian Bollywood playlist, the kind of songs you'd have heard playing on the radio inside a saloon back then."
        }
      },
      {
        "@type": "Question",
        "name": "Is deluxsalon.in the official Deluxe Salon website?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Yes. deluxsalon.in is the Deluxe Salon website — home to the Deluxe Saloon ambient radio experience, its full library of 2000s Bollywood tracks, and the retro barbershop artwork and quotes that go with it."
        }
      },
      {
        "@type": "Question",
        "name": "Where can I listen to the Deluxe Salon songs?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "All the Deluxe Salon songs stream directly in your browser at deluxsalon.in — just press play, no download or sign-up needed. The same tracks are also on YouTube, linked in the site footer."
        }
      },
      {
        "@type": "Question",
        "name": "Is there a Deluxe Salon playlist I can follow?",
        "acceptedAnswer": {
          "@type": "Answer",
          "text": "Yes. The Deluxe Salon playlist plays automatically in shuffle on deluxsalon.in, and you can follow the same playlist on YouTube via the link in the footer."
        }
      }
    ]
  }
  </script>
</head>
<body class="bg-[#120806] text-white selection:bg-amber-500/40">

  <a href="#about" class="sr-only focus:not-sr-only focus:absolute focus:top-2 focus:left-2 focus:z-50 focus:bg-amber-500 focus:text-black focus:px-4 focus:py-2 focus:rounded-full">Skip to content</a>

  <!-- ACTIVE YOUTUBE AUDIO PLAYER BRIDGE (kept in normal flow, with standard 200x200 dimension for YouTube API rendering, visually clipped via overflow/opacity) -->
  <div style="position:fixed;bottom:0;right:0;width:200px;height:200px;opacity:0.001;pointer-events:none;z-index:-10;overflow:hidden;">
    <div id="youtubeBridge"></div>
  </div>

  <!-- ================================================================= -->
  <!-- HERO: full-screen animated saloon scene + player                  -->
  <!-- ================================================================= -->
  <section class="relative w-full h-[100svh] overflow-hidden select-none">

    <!-- 🖼️ EDITABLE BACKGROUND IMAGE — to use a photo instead of the illustration, drop it at img/newbanner.jpg -->
    <div class="absolute inset-0 w-full h-full pointer-events-none z-0 overflow-hidden bg-[#120806]">
      <img id="customBgImage" alt="Illustrated 2000s Indian saloon storefront at golden hour, the visual theme of Deluxe Saloon" class="w-full h-full object-cover" onerror="this.style.display='none'" />
      <script>
        (function () {
          var img = document.getElementById('customBgImage');
          var isMobile = window.matchMedia('(max-width: 640px)').matches
            || /Android|iPhone|iPod|Mobile/i.test(navigator.userAgent);
          img.src = isMobile ? '/img/mobile.png' : '/img/newbanner.jpg';
        })();
      </script>
      <svg viewBox="0 0 1600 900" preserveAspectRatio="xMidYMid slice" class="w-full h-full object-cover" role="img" aria-label="Illustration of a retro Indian saloon storefront at sunset">
        <defs>
          <linearGradient id="skyGrad" x1="0%" y1="0%" x2="0%" y2="100%">
            <stop offset="0%" stop-color="#4a180d" />
            <stop offset="40%" stop-color="#8c2e1b" />
            <stop offset="80%" stop-color="#c24e23" />
            <stop offset="100%" stop-color="#e0722d" />
          </linearGradient>
          <linearGradient id="wallGrad" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#96281b" />
            <stop offset="50%" stop-color="#7a1f14" />
            <stop offset="100%" stop-color="#57120a" />
          </linearGradient>
          <linearGradient id="roofGrad" x1="0%" y1="0%" x2="0%" y2="100%">
            <stop offset="0%" stop-color="#3d0d07" />
            <stop offset="50%" stop-color="#63170c" />
            <stop offset="100%" stop-color="#2b0804" />
          </linearGradient>
          <radialGradient id="sunGlow" cx="20%" cy="30%" r="60%">
            <stop offset="0%" stop-color="#f59e0b" stop-opacity="0.35" />
            <stop offset="50%" stop-color="#d97706" stop-opacity="0.15" />
            <stop offset="100%" stop-color="#000000" stop-opacity="0" />
          </radialGradient>
          <linearGradient id="mirrorReflect" x1="0%" y1="0%" x2="100%" y2="100%">
            <stop offset="0%" stop-color="#ffffff" stop-opacity="0.2" />
            <stop offset="30%" stop-color="#fef3c7" stop-opacity="0.08" />
            <stop offset="100%" stop-color="#000000" stop-opacity="0.4" />
          </linearGradient>
        </defs>

        <rect width="1600" height="900" fill="url(#skyGrad)" />
        <rect width="1600" height="900" fill="url(#sunGlow)" />

        <g opacity="0.6">
          <path d="M -50,550 Q 80,300 200,550 Z" fill="#3b110a" />
          <path d="M 60,320 L 70,300 L 80,320 Z" fill="#3b110a" />
          <path d="M 120,550 Q 180,420 240,550 Z" fill="#2d0d08" />
          <rect x="-100" y="520" width="350" height="200" fill="#2d0d08" />
        </g>

        <g opacity="0.8">
          <path d="M 160,650 Q 180,400 220,200 Q 215,200 150,650 Z" fill="#1c0704" />
          <path d="M 220,200 Q 140,150 80,180" stroke="#1c0704" stroke-width="12" stroke-linecap="round" fill="none" />
          <path d="M 220,200 Q 180,120 120,110" stroke="#1c0704" stroke-width="10" stroke-linecap="round" fill="none" />
          <path d="M 220,200 Q 260,110 320,130" stroke="#1c0704" stroke-width="11" stroke-linecap="round" fill="none" />
          <path d="M 220,200 Q 300,170 360,220" stroke="#1c0704" stroke-width="10" stroke-linecap="round" fill="none" />
          <path d="M 220,200 Q 240,260 280,300" stroke="#1c0704" stroke-width="9" stroke-linecap="round" fill="none" />
        </g>

        <g transform="translate(40, 480)">
          <circle cx="140" cy="220" r="50" stroke="#120503" stroke-width="6" fill="none" />
          <circle cx="280" cy="220" r="50" stroke="#120503" stroke-width="6" fill="none" />
          <path d="M 140,220 L 210,220 L 250,150 L 170,150 Z" stroke="#120503" stroke-width="6" fill="none" />
          <path d="M 210,220 L 210,130 L 190,130" stroke="#120503" stroke-width="6" fill="none" />
          <path d="M 250,150 L 280,220" stroke="#120503" stroke-width="6" fill="none" />

          <rect x="0" y="110" width="130" height="70" fill="#4d1b0d" rx="6" />
          <circle cx="40" cy="200" r="35" stroke="#120503" stroke-width="6" fill="none" />

          <circle cx="25" cy="100" r="12" fill="#ea580c" />
          <circle cx="45" cy="95" r="14" fill="#f97316" />
          <circle cx="68" cy="98" r="13" fill="#ea580c" />
          <circle cx="90" cy="92" r="15" fill="#f97316" />
          <circle cx="110" cy="100" r="11" fill="#ea580c" />
          <circle cx="35" cy="80" r="13" fill="#f97316" />
          <circle cx="58" cy="78" r="12" fill="#eab308" />
          <circle cx="80" cy="80" r="13" fill="#ea580c" />

          <path d="M -10,80 Q -10,180 -10,250" stroke="#1f0905" stroke-width="22" stroke-linecap="round" />
          <circle cx="-10" cy="60" r="16" fill="#1f0905" />
          <path d="M -20,60 Q -10,50 0,60" stroke="#ffffff" stroke-width="3" fill="none" opacity="0.6" />
        </g>

        <g id="saloonShop">
          <rect x="420" y="180" width="850" height="580" fill="url(#wallGrad)" rx="8" />
          <polygon points="380,180 1310,180 1340,240 350,240" fill="url(#roofGrad)" />
          <rect x="480" y="240" width="730" height="460" fill="#2b0a06" rx="4" />
          <rect x="490" y="250" width="710" height="440" fill="#42130b" rx="2" />

          <rect x="620" y="280" width="280" height="300" fill="#1e2e2a" stroke="#8c592b" stroke-width="12" rx="6" />
          <rect x="620" y="280" width="280" height="300" fill="url(#mirrorReflect)" rx="6" />

          <rect x="940" y="300" width="220" height="12" fill="#5c3317" />
          <rect x="940" y="420" width="220" height="12" fill="#5c3317" />

          <rect x="960" y="250" width="22" height="50" fill="#0284c7" opacity="0.8" rx="3" />
          <rect x="990" y="260" width="18" height="40" fill="#16a34a" opacity="0.8" rx="2" />
          <rect x="1015" y="245" width="24" height="55" fill="#d97706" opacity="0.9" rx="4" />
          <rect x="1045" y="255" width="20" height="45" fill="#e11d48" opacity="0.8" rx="3" />

          <rect x="960" y="370" width="26" height="50" fill="#eab308" opacity="0.8" rx="3" />
          <rect x="995" y="380" width="20" height="40" fill="#06b6d4" opacity="0.8" rx="3" />
          <rect x="1025" y="365" width="30" height="55" fill="#f97316" opacity="0.8" rx="4" />

          <rect x="630" y="260" width="260" height="10" fill="#fef3c7" rx="5" />
          <rect x="630" y="260" width="260" height="10" fill="#ffffff" opacity="0.8" rx="5" />
          <polygon points="620,270 900,270 980,500 540,500" fill="#fef3c7" opacity="0.08" />

          <g transform="translate(680, 480)">
            <ellipse cx="60" cy="180" rx="45" ry="14" fill="#a3a3a3" stroke="#525252" stroke-width="3" />
            <rect x="52" y="120" width="16" height="65" fill="#d4d4d4" />
            <rect x="-10" y="100" width="140" height="28" fill="#991b1b" rx="6" stroke="#450a0a" stroke-width="3" />

            <g transform="rotate(-35, 0, 100)">
              <rect x="-20" y="10" width="130" height="40" fill="#b91c1c" rx="8" stroke="#450a0a" stroke-width="3" />
              <rect x="10" y="-15" width="40" height="25" fill="#991b1b" rx="4" />
            </g>

            <g id="customerInChair">
              <path d="M 0,70 Q 60,60 120,95 L 110,120 L -10,120 Z" fill="#f5f5f4" stroke="#d6d3d1" stroke-width="2" />
              <circle cx="-15" cy="40" r="22" fill="#d97706" />
              <path d="M -25,45 Q -15,62 -2,45 Z" fill="#ffffff" />
            </g>

            <g id="barberAtWork" transform="translate(-70, -60)">
              <path d="M 20,80 L 50,80 L 55,200 L 15,200 Z" fill="#1e293b" />
              <path d="M 15,40 L 55,40 L 60,110 L 10,110 Z" fill="#ffffff" />
              <circle cx="35" cy="18" r="18" fill="#b45309" />
              <path d="M 45,55 Q 75,50 90,75" stroke="#ffffff" stroke-width="14" stroke-linecap="round" fill="none" />
              <path d="M 88,72 L 105,78" stroke="#cbd5e1" stroke-width="4" stroke-linecap="round" />
            </g>
          </g>
        </g>

        <rect x="0" y="730" width="1600" height="170" fill="#1c0704" />
        <line x1="0" y1="730" x2="1600" y2="730" stroke="#42130b" stroke-width="8" />
      </svg>
      <div class="absolute inset-0 bg-gradient-to-t from-black/80 via-transparent to-black/50 pointer-events-none"></div>
    </div>

    <!-- RAIN & LIGHTNING CANVAS AND SCREEN FLASH OVERLAY -->
    <canvas id="rainCanvas"></canvas>
    <div id="lightningFlashOverlay"></div>

    <!-- ============================== TOP HEADER NAVIGATION (Responsive & Equal Shape on All Android/Mobile & Desktop) ============================== -->
    <header class="absolute top-2.5 sm:top-4 left-0 right-0 z-30 px-2.5 xs:px-3 sm:px-6 max-w-7xl mx-auto w-full">
      <div class="flex items-center justify-between gap-1.5 xs:gap-2 sm:gap-3 w-full">
        <!-- 1. Online Counter Badge (Equal Height Glassy Pill) -->
        <div class="header-pill flex-1 sm:flex-initial sm:min-w-[105px] px-2 xs:px-3 text-emerald-400 gap-1.5 shrink-0 select-none">
          <span class="w-2 h-2 rounded-full bg-emerald-400 animate-pulse shrink-0 shadow-[0_0_8px_rgba(52,211,153,0.8)]"></span>
          <span id="onlineCount" class="text-white/95 font-mono font-bold tracking-tight">1083</span>
          <span class="text-white/60 text-[10px] font-normal">online</span>
        </div>

        <!-- 2. Theme Selector (Equal Height Glassy Pill) -->
        <div class="theme-select-wrap flex-1 sm:flex-initial sm:w-44 shrink-0">
          <select id="themeSelect" aria-label="Choose a nostalgic 2000s Indian scene" class="theme-select w-full"></select>
        </div>

        <!-- 3. Right side: About, FAQ, Support (Equal Height Glassy Pills) -->
        <nav aria-label="Primary" class="flex items-center gap-1.5 xs:gap-2 flex-1 sm:flex-initial justify-end shrink-0">
          <a href="#about" class="hidden sm:inline-flex header-pill px-3.5">About</a>
          <a href="#faq" class="hidden sm:inline-flex header-pill px-3.5">FAQ</a>
          
          <!-- Support Button (Equal Glassy Pill Shape - always visible on Android/Mobile) -->
          <button id="supportBtn" type="button" aria-label="Open support QR modal" class="header-pill w-full sm:w-auto px-2 xs:px-3 sm:px-3.5 gap-1.5 hover:border-amber-400/50 hover:text-amber-200">
            <span class="text-xs text-red-400 animate-pulse shrink-0" aria-hidden="true">❤️</span>
            <span class="font-semibold text-white/90 text-[11px] xs:text-xs">Support us</span>
          </button>
        </nav>
      </div>
    </header>

    <!-- HERO CENTER CONTENT -->
    <main class="absolute inset-0 z-10 flex flex-col items-center justify-between pt-14 pb-2 xs:pb-3 sm:pt-20 sm:pb-6 px-3 sm:px-6 pointer-events-none">

      <!-- Saloon Title Display -->
      <div class="pointer-events-auto drop-shadow-[0_12px_35px_rgba(0,0,0,0.95)] text-center mt-6 xs:mt-8 sm:mt-0 sm:mb-1">
        <h1 class="font-hindi text-6xl xs:text-7xl sm:text-8xl md:text-9xl lg:text-[9.5rem] font-extrabold tracking-wide text-white leading-[0.88]">
          डीलक्स<br>सैलून
        </h1>

        <!-- Live Chat button -->
        <button id="liveChatToggleBtn" type="button" aria-haspopup="dialog" aria-controls="liveChatPanel" class="tweak-pill group border-amber-500/40 text-amber-300 hover:text-amber-100 hover:border-amber-400 cursor-pointer mt-2.5 mx-auto">
          <span class="w-1.5 h-1.5 rounded-full bg-amber-400 animate-pulse shrink-0" aria-hidden="true"></span>
          <span class="font-medium tracking-wide">💬 Live Chat</span>
          <span id="liveChatBadge" class="live-chat-badge">0</span>
        </button>
      </div>

      <div class="w-full max-w-md xs:max-w-lg sm:max-w-2xl flex flex-col items-center gap-2 sm:gap-3 mt-auto mb-1 sm:my-auto">

        <!-- ============================================================= -->
        <!-- RAIN TOGGLE + WHATSAPP SHARE (same row, directly above player) -->
        <!-- ============================================================= -->
        <div class="order-0 pointer-events-auto flex items-center justify-center gap-1.5 xs:gap-2 flex-nowrap max-w-full overflow-x-auto no-scrollbar py-1 px-1">
          <button id="rainThunderToggleBtn" type="button" aria-pressed="false" class="rain-toggle-pill group shrink-0">
            <span id="rainIcon" class="text-sm transition-transform group-hover:scale-125" aria-hidden="true">🌧️</span>
            <span id="rainText" class="font-medium tracking-wide">Baarish? </span>
            <span class="text-[10px] px-1.5 py-0.5 rounded-full bg-blue-500/20 text-blue-300 font-mono group-hover:bg-blue-500/30">OFF</span>
          </button>

          <a id="instaJoinBtn" href="https://ig.me/j/AbZv1KgcN0W07Rg1/" target="_blank" rel="noopener noreferrer" class="share-toggle-pill group shrink-0">
            <svg class="w-3.5 h-3.5 fill-current text-[#e1306c] shrink-0" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zM12 5.838a6.162 6.162 0 100 12.324 6.162 6.162 0 000-12.324zM12 16a4 4 0 110-8 4 4 0 010 8zm6.406-11.845a1.44 1.44 0 100 2.881 1.44 1.44 0 000-2.881z"/></svg>
            <span class="font-medium tracking-wide whitespace-nowrap"><span class="sm:hidden">Feedback</span><span class="hidden sm:inline">Join Feedback Group</span></span>
          </a>

          <button id="whatsappShareBtn" type="button" class="share-toggle-pill group shrink-0">
            <svg class="w-3.5 h-3.5 fill-current text-[#25D366] shrink-0" viewBox="0 0 24 24" aria-hidden="true">
              <path d="M.057 24l1.687-6.163c-1.041-1.804-1.588-3.849-1.587-5.946.003-6.556 5.338-11.891 11.893-11.891 3.181.001 6.167 1.24 8.413 3.488 2.245 2.248 3.481 5.236 3.48 8.414-.003 6.557-5.338 11.892-11.893 11.892-1.99-.001-3.951-.5-5.688-1.448l-6.305 1.654zm6.597-3.807c1.676.995 3.276 1.591 5.392 1.592 5.448 0 9.886-4.434 9.889-9.885.002-5.462-4.415-9.89-9.881-9.892-5.452 0-9.887 4.434-9.889 9.884-.001 2.225.651 3.891 1.746 5.634l-.999 3.648 3.742-.981zm11.387-5.464c-.074-.124-.272-.198-.57-.347-.297-.149-1.758-.868-2.031-.967-.272-.099-.47-.149-.669.149-.198.297-.768.967-.941 1.165-.173.198-.347.223-.644.074-.297-.149-1.255-.462-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.297-.347.446-.521.151-.172.2-.296.3-.495.099-.198.05-.372-.025-.521-.075-.148-.669-1.611-.916-2.206-.242-.579-.487-.501-.669-.51l-.57-.01c-.198 0-.52.074-.792.372s-1.04 1.016-1.04 2.479 1.065 2.876 1.213 3.074c.149.198 2.095 3.2 5.076 4.487.709.306 1.263.489 1.694.626.712.226 1.36.194 1.872.118.571-.085 1.758-.719 2.006-1.413.248-.695.248-1.29.173-1.414z"/>
            </svg>
            <span class="font-medium tracking-wide">Share</span>
          </button>
        </div>

        <!-- ============================================================= -->
        <!-- PWA INSTALL PROMPT CARD (shown only when browser can install)  -->
        <!-- ============================================================= -->
        <div id="installPromptCard" class="hidden order-1 pointer-events-auto relative w-full max-w-xs xs:max-w-sm sm:max-w-md">
          <div class="relative flex items-center gap-3 px-3.5 py-3 rounded-2xl bg-gradient-to-r from-[#3a0f08]/95 to-[#1c0704]/95 backdrop-blur-2xl border border-amber-500/30 shadow-[0_15px_40px_rgba(0,0,0,0.8)]">
            <img src="/img/icon-192.png" alt="Deluxe Saloon app icon" class="w-10 h-10 rounded-xl shrink-0 border border-white/15 shadow-md object-cover" />
            <div class="min-w-0 flex-1">
              <div class="text-white font-bold text-[13px] sm:text-sm truncate">Install Our APP</div>
              <div class="text-white/60 text-[10.5px] sm:text-xs truncate">One tap listen anywhere.</div>
            </div>
            <button id="installPromptBtn" type="button" class="shrink-0 flex items-center gap-1.5 px-3 py-1.5 rounded-full bg-amber-500 hover:bg-amber-400 text-black font-bold text-[11px] sm:text-xs shadow-md transition-all hover:scale-105 active:scale-95">
              <svg class="w-3.5 h-3.5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M12 3v12m0 0 4-4m-4 4-4-4M5 21h14"/></svg>
              Install
            </button>
            <button id="installPromptCloseBtn" type="button" aria-label="Dismiss install prompt" class="shrink-0 w-6 h-6 rounded-full bg-white/10 hover:bg-white/20 text-white/60 hover:text-white flex items-center justify-center text-sm leading-none transition-colors">
              &times;
            </button>
          </div>
        </div>

        <!-- VINTAGE MUSIC PLAYER (positioned right down at the end of mobile screen) -->
        <section class="music-player order-2 sm:order-2 pointer-events-auto shadow-[0_25px_60px_rgba(0,0,0,0.85)] w-full" aria-label="Deluxe Saloon YouTube playlist music player">
          <button class="music-cover-frame" id="coverButton" type="button" aria-label="Play from cover art">
            <div class="music-cover" id="coverDisc" style="background-image: url('https://img.youtube.com/vi/J2z_a0vLd-U/hqdefault.jpg');"></div>
            <span class="music-cover-hole" aria-hidden="true"></span>
          </button>
          <div class="track-block">
            <div class="track-top">
              <div class="track-name" id="trackName">Nostalgia Loading...</div>
            </div>
            <p class="station">Credits: <span id="channelName">YouTube</span></p>
            <div class="progress-row">
              <span id="timeNow">0:00</span>
              <div class="progress" id="progressBar" role="slider" aria-label="Track progress" aria-valuemin="0" aria-valuemax="100" aria-valuenow="0" tabindex="0">
                <div class="progress-fill" id="progressFill"></div>
              </div>
              <span id="timeTotal">0:00</span>
            </div>
          </div>
          <div class="player-actions">
            <button class="icon-button" id="prevButton" type="button" aria-label="Previous track">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m19 20-9-8 9-8v16Z"></path><path d="M5 19V5"></path></svg>
            </button>
            <button class="icon-button play-button" id="playButton" type="button" aria-label="Play music">
              <svg id="playIcon" viewBox="0 0 24 24" aria-hidden="true"><path d="M8 5v14l11-7-11-7Z"></path></svg>
            </button>
            <button class="icon-button" id="nextButton" type="button" aria-label="Next track">
              <svg viewBox="0 0 24 24" aria-hidden="true"><path d="m5 4 9 8-9 8V4Z"></path><path d="M19 5v14"></path></svg>
            </button>
            <div class="relative">
              <button class="icon-button" id="volumeButton" type="button" aria-label="Volume">
                <svg id="volumeIcon" viewBox="0 0 24 24" aria-hidden="true"><path d="M11 5 6 9H2v6h4l5 4V5Z"></path><path d="M15.5 8.5a5 5 0 0 1 0 7"></path><path d="M18.5 5.5a9 9 0 0 1 0 13"></path></svg>
              </button>
              <div id="volumePopover" class="hidden absolute bottom-full right-0 mb-2 px-3 py-2.5 rounded-xl bg-[#1c0704] border border-amber-500/30 shadow-lg z-10">
                <input type="range" id="volumeSlider" min="0" max="100" value="100" aria-label="Volume level" class="w-24 accent-amber-400" />
              </div>
            </div>
          </div>
        </section>

        <footer class="order-3 sm:order-3 pointer-events-auto text-[8.5px] sm:text-xs text-white/50 tracking-widest font-mono mt-0 sm:mt-1">
          contact: pr@rjmedia.in
        </footer>
      </div>

      <a href="#about" aria-label="Scroll to learn more about Deluxe Saloon" class="scroll-cue pointer-events-auto hidden sm:flex absolute bottom-3 left-1/2 -translate-x-1/2 flex-col items-center gap-1 text-white/60 hover:text-amber-300 transition-colors">
        <span class="text-[10px] tracking-[0.25em] uppercase">Scroll</span>
        <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M12 5v14M5 12l7 7 7-7"/></svg>
      </a>
    </main>
  </section>

  <!-- ============================================================= -->
  <!-- 1. SUPPORT POPUP MODAL (Manual click + 5-Minute Auto Popup)     -->
  <!-- ============================================================= -->
  <div id="supportModal" class="app-modal" role="dialog" aria-modal="true" aria-labelledby="supportModalTitle">
    <div class="app-modal-card">
      <button id="supportModalClose" type="button" aria-label="Close" class="absolute top-4 right-4 w-9 h-9 rounded-full bg-white/10 hover:bg-white/20 text-white/70 hover:text-white text-xl flex items-center justify-center transition-colors">&times;</button>
      
      <div class="w-12 h-12 rounded-full bg-amber-500/20 border border-amber-500/40 flex items-center justify-center mx-auto mb-3 text-2xl">
        ❤️
      </div>

      <h2 id="supportModalTitle" class="text-xl sm:text-2xl font-bold text-white mb-2">No ads - Just memories. Support the Platform to Stay FOREVER</h2>
      <p class="text-white/70 text-xs sm:text-sm leading-relaxed mb-5 max-w-md mx-auto">
    With over 1 Lac daily visitors, We promise never to put ads and ruin your experience. But web server costs are high to keep this website smooth, Please send any amount you wish. Thank you in advance! ❤️️
      </p>
      
      <div class="flex justify-center my-2">
        <img id="qrImage" src="img/qr.jpg" alt="QR code to support Deluxe Saloon" class="w-full max-w-[220px] sm:max-w-[280px] aspect-square object-contain rounded-2xl border border-white/15 bg-white p-3 shadow-2xl" />
      </div>

      <a id="qrDownloadBtn" href="img/qr.jpg" download="deluxe-saloon-support-qr.png" class="mt-4 inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-amber-500 hover:bg-amber-400 text-black font-bold text-xs sm:text-sm transition-all shadow-lg hover:scale-105 active:scale-95">
        <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M12 3v12m0 0 4-4m-4 4-4-4M5 21h14"/></svg>
        Download QR Code
      </a>
      
      <p class="text-white/40 text-[11px] mt-3">Save it and scan with any UPI app.</p>
      <p id="qrFallbackNote" class="hidden text-white/40 text-xs"> (GPay / PhonePe / Paytm) </p>

      <button id="supportModalDismissBtn" type="button" class="mt-4 text-xs text-white/50 hover:text-white underline">Dismiss</button>
    </div>
  </div>

  <!-- ============================================================= -->
  <!-- LIVE CHAT PANEL                                                 -->
  <!-- ============================================================= -->
  <div id="liveChatPanel" role="dialog" aria-modal="true" aria-labelledby="liveChatTitle">
    <div class="chat-card">
      <div class="flex items-center justify-between px-4 py-3 border-b border-white/10">
        <div class="flex items-center gap-2">
          <span class="w-2 h-2 rounded-full bg-amber-400 animate-pulse" aria-hidden="true"></span>
          <h2 id="liveChatTitle" class="text-white font-bold text-sm">Live Chat</h2>
        </div>
        <button id="liveChatCloseBtn" type="button" aria-label="Close live chat" class="w-8 h-8 rounded-full bg-white/10 hover:bg-white/20 text-white/70 hover:text-white text-lg flex items-center justify-center transition-colors">&times;</button>
      </div>

      <div id="chatMessages" class="chat-messages"></div>
      <div id="chatErrorBanner" class="hidden mx-3 mb-2 px-3 py-2 rounded-lg bg-red-500/15 border border-red-500/30 text-red-300 text-[11px] leading-relaxed"></div>

      <!-- Name capture: replaces the composer for one step, only the very
           first time a visitor sends a message. -->
      <div id="chatNamePrompt" class="hidden px-3 pb-3 border-t border-white/10 pt-3">
        <p class="text-amber-300 text-[11px] font-semibold mb-1 px-1">One last step — what's your name?</p>
        <p class="text-white/50 text-[11px] mb-2 px-1">We'll send this as soon as you enter it: <span id="chatPendingPreview" class="text-white/80 italic"></span></p>
        <div class="flex gap-2">
          <input id="chatNameInput" type="text" maxlength="40" placeholder="Your name" class="flex-1 px-3 py-2 rounded-xl bg-black/50 border border-amber-400/40 focus:border-amber-400 focus:outline-none text-white placeholder-white/40 text-sm" />
          <button id="chatNameSaveBtn" type="button" class="px-4 py-2 rounded-xl bg-amber-500 hover:bg-amber-400 text-black font-bold text-sm transition-all">Send</button>
        </div>
        <button id="chatNameCancelBtn" type="button" class="mt-2 text-[11px] text-white/40 hover:text-white/70 underline">Cancel</button>
      </div>

      <!-- Message composer -->
      <form id="chatSendForm" class="flex items-center gap-2 px-3 py-3 border-t border-white/10">
        <input id="chatMessageInput" type="text" maxlength="300" placeholder="Type a message..." autocomplete="off" class="flex-1 px-3 py-2 rounded-xl bg-black/50 border border-white/15 focus:border-amber-400 focus:outline-none text-white placeholder-white/40 text-sm" />
        <button id="chatSendBtn" type="submit" class="w-10 h-10 shrink-0 rounded-full bg-amber-500 hover:bg-amber-400 text-black flex items-center justify-center transition-all active:scale-95" aria-label="Send message">
          <svg class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" aria-hidden="true"><path d="m22 2-7 20-4-9-9-4Z"/><path d="M22 2 11 13"/></svg>
        </button>
      </form>
    </div>
  </div>

  <!-- ================================================================= -->
  <!-- CRAWLABLE CONTENT — real static text so search engines have       -->
  <!-- something to index beyond the animated hero.                     -->
  <!-- ================================================================= -->
  <section id="about" class="relative bg-[#1c0704] border-t border-amber-500/10 px-5 sm:px-8 py-16 sm:py-24">
    <div class="max-w-3xl mx-auto text-center">
      <p class="text-amber-400 text-xs tracking-[0.3em] uppercase font-semibold mb-3">Welcome to</p>
      <h2 class="font-display text-3xl sm:text-5xl text-white mb-6">Deluxe Saloon — a 2000s Indian Saloon, Rebuilt in Sound</h2>
      <p class="text-white/70 leading-relaxed text-sm sm:text-base">
        Deluxe Saloon (also spelled <strong class="text-white">Deluxe Salon</strong> or <strong class="text-white">Delux Salon</strong>) is a free ambient radio built at <a href="https://deluxsalon.in" class="text-amber-300 underline decoration-amber-500/40 hover:decoration-amber-300">deluxsalon.in</a> to recreate one very specific memory: the neighbourhood hair-cutting saloon of 2000s India, with a battered radio in the corner playing Bollywood hits between the buzz of clippers and the snip of scissors. Press play, and Deluxe Saloon streams a nonstop mix of 2000s Bollywood radio nostalgia, styled around the sights, sounds and one-liners of a classic Indian barbershop.
      </p>
    </div>

    <div class="max-w-5xl mx-auto grid sm:grid-cols-3 gap-5 sm:gap-6 mt-14">
      <div class="rounded-2xl bg-white/5 border border-white/10 p-6 text-left">
        <div class="w-10 h-10 rounded-full bg-amber-500/15 border border-amber-500/30 flex items-center justify-center mb-4" aria-hidden="true">
          <svg class="w-5 h-5 text-amber-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/></svg>
        </div>
        <h3 class="text-white font-semibold mb-2">2000s Bollywood Radio</h3>
        <p class="text-white/60 text-sm leading-relaxed">A curated playlist of the era's biggest saloon-radio staples, tuned to keep playing in the background all day.</p>
      </div>
      <div class="rounded-2xl bg-white/5 border border-white/10 p-6 text-left">
        <div class="w-10 h-10 rounded-full bg-amber-500/15 border border-amber-500/30 flex items-center justify-center mb-4" aria-hidden="true">
          <svg class="w-5 h-5 text-amber-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 2a3 3 0 0 0-3 3v6a3 3 0 0 0 6 0V5a3 3 0 0 0-3-3Z"/><path d="M19 10v1a7 7 0 0 1-14 0v-1M12 18v4"/></svg>
        </div>
        <h3 class="text-white font-semibold mb-2">Barber Chatter &amp; Quotes</h3>
        <p class="text-white/60 text-sm leading-relaxed">Rotating one-liners from Deluxe Saloon's cast of barbers, woven into the retro soundtrack.</p>
      </div>
      <div class="rounded-2xl bg-white/5 border border-white/10 p-6 text-left">
        <div class="w-10 h-10 rounded-full bg-amber-500/15 border border-amber-500/30 flex items-center justify-center mb-4" aria-hidden="true">
          <svg class="w-5 h-5 text-amber-400" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 12a9 9 0 1 0 18 0 9 9 0 0 0-18 0Z"/><path d="M12 7v5l3 3"/></svg>
        </div>
        <h3 class="text-white font-semibold mb-2">Always Open</h3>
        <p class="text-white/60 text-sm leading-relaxed">Deluxe Saloon is free, streams straight from your browser, and never closes — no sign-up, no app.</p>
      </div>
    </div>
  </section>

  <section id="faq" class="relative bg-[#120806] px-5 sm:px-8 py-16 sm:py-24 border-t border-white/5">
    <div class="max-w-2xl mx-auto">
      <p class="text-amber-400 text-xs tracking-[0.3em] uppercase font-semibold mb-3 text-center">FAQ</p>
      <h2 class="font-display text-3xl sm:text-4xl text-white mb-10 text-center">Deluxe Saloon, Explained</h2>

      <div class="space-y-3">
        <details class="faq-item group rounded-xl bg-white/5 border border-white/10 p-4 sm:p-5" open>
          <summary class="flex items-center justify-between cursor-pointer text-white font-medium text-sm sm:text-base list-none">
            What is Deluxe Saloon?
            <svg class="faq-chevron w-4 h-4 text-amber-400 transition-transform" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="m6 9 6 6 6-6"/></svg>
          </summary>
          <p class="text-white/60 text-sm mt-3 leading-relaxed">Deluxe Saloon — also written Deluxe Salon or Delux Salon — is a free ambient radio website at deluxsalon.in that recreates the sound and mood of a classic 2000s Indian neighbourhood barbershop, complete with nostalgic Bollywood tunes and barber chatter.</p>
        </details>
        <details class="faq-item group rounded-xl bg-white/5 border border-white/10 p-4 sm:p-5">
          <summary class="flex items-center justify-between cursor-pointer text-white font-medium text-sm sm:text-base list-none">
            Is Deluxe Saloon free to use?
            <svg class="faq-chevron w-4 h-4 text-amber-400 transition-transform" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="m6 9 6 6 6-6"/></svg>
          </summary>
          <p class="text-white/60 text-sm mt-3 leading-relaxed">Yes. Deluxe Saloon streams entirely for free in your browser, with no sign-up required, on both desktop and mobile.</p>
        </details>
        <details class="faq-item group rounded-xl bg-white/5 border border-white/10 p-4 sm:p-5">
          <summary class="flex items-center justify-between cursor-pointer text-white font-medium text-sm sm:text-base list-none">
            What kind of music plays on Deluxe Saloon?
            <svg class="faq-chevron w-4 h-4 text-amber-400 transition-transform" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="m6 9 6 6 6-6"/></svg>
          </summary>
          <p class="text-white/60 text-sm mt-3 leading-relaxed">Deluxe Saloon plays a curated 2000s Indian Bollywood playlist — the kind of songs you'd have heard on the radio inside a saloon back then. The same playlist is also available on YouTube, linked in the footer below.</p>
        </details>
        <details class="faq-item group rounded-xl bg-white/5 border border-white/10 p-4 sm:p-5">
          <summary class="flex items-center justify-between cursor-pointer text-white font-medium text-sm sm:text-base list-none">
            Is deluxsalon.in the official Deluxe Salon website?
            <svg class="faq-chevron w-4 h-4 text-amber-400 transition-transform" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="m6 9 6 6 6-6"/></svg>
          </summary>
          <p class="text-white/60 text-sm mt-3 leading-relaxed">Yes. deluxsalon.in is the Deluxe Salon website — home to the Deluxe Saloon ambient radio experience, its full library of 2000s Bollywood tracks, and the retro barbershop artwork and quotes that go with it.</p>
        </details>
        <details class="faq-item group rounded-xl bg-white/5 border border-white/10 p-4 sm:p-5">
          <summary class="flex items-center justify-between cursor-pointer text-white font-medium text-sm sm:text-base list-none">
            Where can I listen to the Deluxe Salon songs?
            <svg class="faq-chevron w-4 h-4 text-amber-400 transition-transform" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="m6 9 6 6 6-6"/></svg>
          </summary>
          <p class="text-white/60 text-sm mt-3 leading-relaxed">All the Deluxe Salon songs stream directly in your browser at deluxsalon.in — just press play, no download or sign-up needed. The same tracks are also on YouTube, linked in the footer.</p>
        </details>
        <details class="faq-item group rounded-xl bg-white/5 border border-white/10 p-4 sm:p-5">
          <summary class="flex items-center justify-between cursor-pointer text-white font-medium text-sm sm:text-base list-none">
            Is there a Deluxe Salon playlist I can follow?
            <svg class="faq-chevron w-4 h-4 text-amber-400 transition-transform" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="m6 9 6 6 6-6"/></svg>
          </summary>
          <p class="text-white/60 text-sm mt-3 leading-relaxed">Yes. The Deluxe Salon playlist plays automatically in shuffle on deluxsalon.in, and you can follow the same playlist on YouTube via the link in the footer.</p>
        </details>
      </div>
    </div>
  </section>

  <footer class="bg-[#0d0503] border-t border-white/5 px-5 sm:px-8 py-8 text-center">
    <p class="font-hindi text-lg text-amber-200/80 mb-1">डीलक्स सैलून</p>
    <p class="text-white/40 text-xs">Deluxe Saloon · deluxsalon.in </p>
    <div class="flex items-center justify-center gap-3 mt-4">
      <a href="https://whatsapp.com/channel/0029VajyupYAjPXTOvgg2Z41" target="_blank" rel="noopener noreferrer" aria-label="Join Deluxe Saloon WhatsApp Channel" class="h-9 px-3.5 rounded-lg bg-[#25D366]/15 hover:bg-[#25D366]/25 border border-[#25D366]/40 hover:border-[#25D366] text-[#25D366] hover:text-emerald-300 flex items-center justify-center gap-2 text-xs font-semibold tracking-wide transition-all shadow-sm active:scale-95">
        <svg class="w-4 h-4 fill-current shrink-0" viewBox="0 0 24 24" aria-hidden="true">
          <path d="M.057 24l1.687-6.163c-1.041-1.804-1.588-3.849-1.587-5.946.003-6.556 5.338-11.891 11.893-11.891 3.181.001 6.167 1.24 8.413 3.488 2.245 2.248 3.481 5.236 3.48 8.414-.003 6.557-5.338 11.892-11.893 11.892-1.99-.001-3.951-.5-5.688-1.448l-6.305 1.654zm6.597-3.807c1.676.995 3.276 1.591 5.392 1.592 5.448 0 9.886-4.434 9.889-9.885.002-5.462-4.415-9.89-9.881-9.892-5.452 0-9.887 4.434-9.889 9.884-.001 2.225.651 3.891 1.746 5.634l-.999 3.648 3.742-.981zm11.387-5.464c-.074-.124-.272-.198-.57-.347-.297-.149-1.758-.868-2.031-.967-.272-.099-.47-.149-.669.149-.198.297-.768.967-.941 1.165-.173.198-.347.223-.644.074-.297-.149-1.255-.462-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.297-.347.446-.521.151-.172.2-.296.3-.495.099-.198.05-.372-.025-.521-.075-.148-.669-1.611-.916-2.206-.242-.579-.487-.501-.669-.51l-.57-.01c-.198 0-.52.074-.792.372s-1.04 1.016-1.04 2.479 1.065 2.876 1.213 3.074c.149.198 2.095 3.2 5.076 4.487.709.306 1.263.489 1.694.626.712.226 1.36.194 1.872.118.571-.085 1.758-.719 2.006-1.413.248-.695.248-1.29.173-1.414z"/>
        </svg>
        <span>Join Group</span>
      </a>
      <a href="https://www.instagram.com/deluxsalon.in" target="_blank" rel="noopener" aria-label="Deluxe Saloon on Instagram" class="w-9 h-9 rounded-full bg-white/5 hover:bg-white/10 border border-white/10 flex items-center justify-center transition-colors">
        <svg class="w-4 h-4 text-pink-400 fill-current" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zM12 0C8.741 0 8.333.014 7.053.072 2.695.272.273 2.69.073 7.052.014 8.333 0 8.741 0 12c0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98C8.333 23.986 8.741 24 12 24c3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98C15.668.014 15.259 0 12 0Zm0 5.838a6.162 6.162 0 1 0 0 12.324 6.162 6.162 0 0 0 0-12.324ZM12 16a4 4 0 1 1 0-8 4 4 0 0 1 0 8Zm6.406-11.845a1.44 1.44 0 1 0 0 2.881 1.44 1.44 0 0 0 0-2.881Z"/></svg>
      </a>
      <a href="https://www.youtube.com/@deluxsalonplaylist?sub_confirmation=1" target="_blank" rel="noopener" aria-label="Deluxe Saloon on YouTube" class="w-9 h-9 rounded-full bg-white/5 hover:bg-white/10 border border-white/10 flex items-center justify-center transition-colors">
        <svg class="w-4 h-4 text-red-500 fill-current" viewBox="0 0 24 24" aria-hidden="true"><path d="M12 0C5.376 0 0 5.376 0 12s5.376 12 12 12 12-5.376 12-12S18.624 0 12 0zm0 19.2c-3.96 0-7.2-3.24-7.2-7.2S8.04 4.8 12 4.8s7.2 3.24 7.2 7.2-3.24 7.2-7.2 7.2zm0-11.4c-2.32 0-4.2 1.88-4.2 4.2s1.88 4.2 4.2 4.2 4.2-1.88 4.2-4.2-1.88-4.2-4.2-4.2zm-1.2 6.3V9.9l3.6 2.1-3.6 2.1z"/></svg>
      </a>
    </div>
    <p class="text-white/40 text-[11.5px] leading-relaxed max-w-xl mx-auto mt-6 px-4">
      Disclaimer: Audio plays through YouTube’s embedded player. Nothing is hosted on this site, and all rights stay with the labels, composers and performers. Song credits are put together from film soundtrack listings.
    </p>
    <p class="text-white/50 text-xs sm:text-[13px] font-medium mt-4">
      Powered By <a href="https://rjmedia.in" target="_blank" rel="noopener noreferrer" class="font-bold text-amber-400 hover:text-amber-300 underline decoration-amber-400/60">RJ Media</a>
    </p>
    <p class="text-white/35 text-[11px] font-mono mt-2">contact: pr@rjmedia.in</p>
  </footer>

  <!-- ============================== PWA: service worker + native install prompt ============================== -->
  <script>
    if ('serviceWorker' in navigator) {
      window.addEventListener('load', () => {
        navigator.serviceWorker.register('/sw.js').catch((err) => {
          console.warn('SW registration failed:', err);
        });
      });
    }

    let deferredInstallPrompt = null;
    const installCard = document.getElementById('installPromptCard');
    const installBtn = document.getElementById('installPromptBtn');
    const installCloseBtn = document.getElementById('installPromptCloseBtn');

    window.addEventListener('beforeinstallprompt', (e) => {
      e.preventDefault();
      deferredInstallPrompt = e;
      if (localStorage.getItem('deluxe_install_dismissed') !== 'true' && installCard) {
        installCard.classList.remove('hidden');
      }
    });

    installBtn?.addEventListener('click', async () => {
      if (!deferredInstallPrompt) return;
      deferredInstallPrompt.prompt();
      const installChoice = await deferredInstallPrompt.userChoice;
      if (typeof gtag === 'function') {
        gtag('event', 'pwa_install_prompt_outcome', { outcome: installChoice.outcome });
      }
      deferredInstallPrompt = null;
      installCard?.classList.add('hidden');
    });

    installCloseBtn?.addEventListener('click', () => {
      installCard?.classList.add('hidden');
      localStorage.setItem('deluxe_install_dismissed', 'true');
    });

    window.addEventListener('appinstalled', () => {
      if (typeof gtag === 'function') {
        gtag('event', 'pwa_install_completed');
      }
      installCard?.classList.add('hidden');
      deferredInstallPrompt = null;
    });

    (function () {
      const isStandalone = window.matchMedia('(display-mode: standalone)').matches
        || window.navigator.standalone === true;
      if (typeof gtag === 'function') {
        gtag('event', 'pwa_session', { app_mode: isStandalone ? 'standalone' : 'browser' });
      }
    })();
  </script>

  <!-- ============================== SIMPLE WHATSAPP SHARE (no gating, alternates 2 messages) ============================== -->
  <script>
    (function () {
      const shareBtn = document.getElementById('whatsappShareBtn');
      if (!shareBtn) return;

      const shareMessages = [
        "*क्या आपको बचपन वाले सैलून के पुराने सुपरहिट गाने याद हैं? 📻*\n\n" +
        "वही पुरानी यादें और नॉन-स्टॉप म्यूज़िक, बिना किसी रुकावट के। अब बस एक click दूर✨\n\n" +
        "*पसंद आए तो शेयर जरूर करे ❤️*\n" +
        "👉🏻 https://deluxsalon.in",

        "*Do you remember those classic superhit songs from your childhood hair cut? 📻*\n\n" +
        "The same old nostalgia and non-stop music, without any interruptions. Now just one click away✨\n\n" +
        "*If you like it, please share! ❤️*\n" +
        "👉🏻 https://deluxsalon.in"
      ];

      let msgIndex = Math.floor(Math.random() * shareMessages.length);

      shareBtn.addEventListener('click', () => {
        const message = encodeURIComponent(shareMessages[msgIndex % shareMessages.length]);
        msgIndex++;
        window.open(`https://api.whatsapp.com/send?text=${message}`, '_blank');
      });
    })();
  </script>

  <!-- GOOGLE YOUTUBE IFRAME PLAYER API -->
  <script src="https://www.youtube.com/iframe_api"></script>

  <!-- ============================== SCENE DROPDOWN (redirects to standalone theme pages) ============================== -->
  <script>
    (function () {
      const FALLBACK_THEMES = [{ id: 'saloon', emoji: '💈', label: 'Saloon', path: '/' }];
      const sel = document.getElementById('themeSelect');
      if (!sel) return;

      function populate(list) {
        const options = list.map(t => `<option value="${t.path}">${t.emoji || ''} ${t.label}</option>`).join('');
        sel.innerHTML = `<option value="" disabled selected hidden>Change Theme</option>${options}`;
      }

      fetch('/themes.json', { cache: 'no-store' })
        .then(r => { if (!r.ok) throw new Error('themes.json not found'); return r.json(); })
        .then(data => populate(Array.isArray(data.options) && data.options.length ? data.options : FALLBACK_THEMES))
        .catch(() => populate(FALLBACK_THEMES));

      sel.addEventListener('change', () => {
        if (sel.value) window.location.href = sel.value;
      });
    })();
  </script>

  <!-- ============================== SUPPORT POPUP LOGIC ============================== -->
  <script>
    (function () {
      const btn = document.getElementById('supportBtn');
      const modal = document.getElementById('supportModal');
      const closeBtn = document.getElementById('supportModalClose');
      const dismissBtn = document.getElementById('supportModalDismissBtn');
      const qrImg = document.getElementById('qrImage');
      const qrDownloadBtn = document.getElementById('qrDownloadBtn');
      const qrFallbackNote = document.getElementById('qrFallbackNote');
      if (!modal) return;

      if (qrImg) {
        qrImg.addEventListener('error', () => {
          qrImg.style.display = 'none';
          if (qrDownloadBtn) qrDownloadBtn.style.display = 'none';
          if (qrFallbackNote) qrFallbackNote.classList.remove('hidden');
        });
      }

      function openModal(isAuto = false) {
        modal.classList.add('open');
        document.body.style.overflow = 'hidden';
        if (isAuto) {
          localStorage.setItem('support_popup_last_shown', Date.now().toString());
        }
      }
      function closeModal() {
        modal.classList.remove('open');
        document.body.style.overflow = '';
      }

      window.openSupportModal = () => openModal(false);
      window.closeSupportModal = closeModal;

      btn?.addEventListener('click', () => openModal(false));
      closeBtn?.addEventListener('click', closeModal);
      dismissBtn?.addEventListener('click', closeModal);
      modal.addEventListener('click', (e) => { if (e.target === modal) closeModal(); });
      document.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeModal(); });

      // ROBUST 5-MINUTE AUTO SUPPORT POPUP (With 30-minute cooldown & refresh safety)
      (function initSupportTimer() {
        const FIVE_MINUTES_MS = 5 * 60 * 1000;
        const COOLDOWN_30_MIN_MS = 30 * 60 * 1000;
        const pageLoadTime = Date.now();

        function checkSupportPopup() {
          const now = Date.now();
          const lastShown = parseInt(localStorage.getItem('support_popup_last_shown') || '0', 10);
          if (lastShown && now - lastShown < COOLDOWN_30_MIN_MS) {
            return;
          }
          const timeOnCurrentPage = now - pageLoadTime;
          if (timeOnCurrentPage >= FIVE_MINUTES_MS) {
            openModal(true);
          }
        }

        const timerId = setInterval(() => {
          const now = Date.now();
          const lastShown = parseInt(localStorage.getItem('support_popup_last_shown') || '0', 10);
          if (lastShown && now - lastShown < COOLDOWN_30_MIN_MS) {
            return;
          }
          if (now - pageLoadTime >= FIVE_MINUTES_MS) {
            clearInterval(timerId);
            openModal(true);
          }
        }, 2000);

        document.addEventListener('visibilitychange', () => {
          if (document.visibilityState === 'visible') {
            checkSupportPopup();
          }
        });
      })();
    })();
  </script>

  <!-- ============================== JAVASCRIPT PLAYER LOGIC ============================== -->
  <script>
    const playlistId = "PLVFLMYM1tErk";
    let ytPlayer = null;
    let isPlaying = false;
    let progressTimer = null;
    let metadataPollTimer = null;
    let isPreloadPass = true;

    function formatTime(s) {
      if (!s || isNaN(s)) return '0:00';
      const m = Math.floor(s / 60);
      const sec = Math.floor(s % 60);
      return `${m}:${sec < 10 ? '0' : ''}${sec}`;
    }

    // Starting index used while the player boots up — this gets overridden
    // by startAtRandomIndex() below once the real, full playlist length is
    // known, so the very first song is randomized across the WHOLE
    // playlist rather than only this small initial range.
    const initialRandomIndex = Math.floor(Math.random() * 40);

    function instantiateYTPlayer() {
      if (ytPlayer || !window.YT || !window.YT.Player) return;
      try {
        ytPlayer = new YT.Player('youtubeBridge', {
          height: '200',
          width: '200',
          playerVars: {
            listType: 'playlist',
            list: playlistId,
            index: initialRandomIndex,
            autoplay: 0,
            controls: 0,
            enablejsapi: 1,
            playsinline: 1,
            rel: 0,
            origin: window.location.origin
          },
          events: {
            'onReady': onPlayerReady,
            'onStateChange': onPlayerStateChange,
            'onError': onPlayerError
          }
        });
      } catch (e) {
        console.warn('YT Player init error:', e);
      }
    }

    window.onYouTubeIframeAPIReady = function() {
      instantiateYTPlayer();
    };

    if (window.YT && window.YT.Player) {
      instantiateYTPlayer();
    }

    // Runs once per page load: picks one genuinely random song out of the
    // FULL playlist to start on. After this single jump, Next/Previous just
    // move sequentially forward/backward through the playlist in order —
    // no re-shuffling on every skip.
    function startAtRandomIndex(attempt) {
      attempt = attempt || 0;
      if (!ytPlayer || typeof ytPlayer.getPlaylist !== 'function') return;
      const list = ytPlayer.getPlaylist();
      const total = (list && list.length) || 0;
      if (!total) {
        if (attempt < 20) setTimeout(() => startAtRandomIndex(attempt + 1), 250);
        return;
      }
      const randomIndex = Math.floor(Math.random() * total);
      try {
        ytPlayer.playVideoAt(randomIndex);
      } catch (e) {}
    }

    function onPlayerReady(event) {
      try { event.target.setVolume(100); } catch (e) {}
      // Loop back to song 1 after the last track instead of stopping,
      // since we're now playing straight through in playlist order.
      try { event.target.setLoop(true); } catch (e) {}

      // Silent preload: muting + playing (then immediately pausing, once
      // this lands as a real PLAYING event below) forces YouTube to load
      // title/channel/thumbnail right away, so credits show before the
      // user ever taps Play. This same call also performs the one-time
      // random jump to a full-playlist random starting song.
      try { event.target.mute(); } catch (e) {}
      startAtRandomIndex(0);

      startMetadataPolling();
    }

    function onPlayerError(e) {
      console.warn('YouTube playback error (code ' + (e && e.data) + '). Skipping to next track...');
      nextTrack();
    }

    function startMetadataPolling() {
      if (metadataPollTimer) clearInterval(metadataPollTimer);
      let attempts = 0;
      metadataPollTimer = setInterval(() => {
        attempts++;
        const hasTitle = updateTrackData();
        if (hasTitle || attempts > 25) {
          clearInterval(metadataPollTimer);
        }
      }, 250);
    }

    function onPlayerStateChange(event) {
      if (event.data === YT.PlayerState.PLAYING) {
        if (isPreloadPass) {
          // This PLAYING event came from the silent muted preload, not a
          // real user tap — pause it back and restore audio, but don't mark
          // the UI as "playing" for a song the user never actually started.
          isPreloadPass = false;
          try {
            ytPlayer.pauseVideo();
            ytPlayer.unMute();
            ytPlayer.setVolume(100);
          } catch (e) {}
          updateTrackData();
          return;
        }
        isPlaying = true;
        updatePlayIconUI(true);
        updateTrackData();
        startProgressMonitor();
      } else if (event.data === YT.PlayerState.PAUSED) {
        isPlaying = false;
        updatePlayIconUI(false);
      } else if (event.data === YT.PlayerState.ENDED) {
        nextTrack();
      } else if (event.data === YT.PlayerState.CUED || event.data === YT.PlayerState.BUFFERING) {
        updateTrackData();
      }
    }

    function updateTrackData() {
      if (!ytPlayer || typeof ytPlayer.getVideoData !== 'function') return false;
      try {
        const data = ytPlayer.getVideoData();
        let updated = false;
        if (data && data.title && data.title.trim() !== "") {
          const trackEl = document.getElementById('trackName');
          if (trackEl && trackEl.innerText !== data.title) {
            trackEl.innerText = data.title;
          }
          updated = true;
        }
        if (data && data.author && data.author.trim() !== "") {
          const channelEl = document.getElementById('channelName');
          if (channelEl && channelEl.innerText !== data.author) {
            channelEl.innerText = data.author;
          }
        }
        if (data && data.video_id) {
          const disc = document.getElementById('coverDisc');
          if (disc) disc.style.backgroundImage = `url('https://img.youtube.com/vi/${data.video_id}/hqdefault.jpg')`;
        }
        return updated;
      } catch(e) {
        return false;
      }
    }

    function startProgressMonitor() {
      if (progressTimer) clearInterval(progressTimer);
      progressTimer = setInterval(() => {
        if (ytPlayer && ytPlayer.getCurrentTime && ytPlayer.getDuration) {
          const cur = ytPlayer.getCurrentTime() || 0;
          const dur = ytPlayer.getDuration() || 0;
          document.getElementById('timeNow').innerText = formatTime(cur);
          document.getElementById('timeTotal').innerText = formatTime(dur);
          const pct = dur > 0 ? (cur / dur) * 100 : 0;
          const fill = document.getElementById('progressFill');
          if (fill) fill.style.width = `${pct}%`;
          const pBar = document.getElementById('progressBar');
          if (pBar) pBar.setAttribute('aria-valuenow', Math.round(pct));
          updateTrackData();
        }
      }, 500);
    }

    function updatePlayIconUI(playing) {
      const icon = document.getElementById('playIcon');
      if (icon) {
        icon.innerHTML = playing
          ? `<path d="M6 19h4V5H6v14zm8-14v14h4V5h-4z"></path>`
          : `<path d="M8 5v14l11-7-11-7Z"></path>`;
      }
      const btn = document.getElementById('playButton');
      if (btn) btn.setAttribute('aria-label', playing ? 'Pause music' : 'Play music');
      const disc = document.getElementById('coverDisc');
      if (disc) disc.classList.toggle('spinning', playing);
    }

    function togglePlay() {
      if (!ytPlayer) {
        instantiateYTPlayer();
        return;
      }
      try {
        if (isPlaying) {
          ytPlayer.pauseVideo();
        } else {
          ytPlayer.playVideo();
          updateTrackData();
        }
      } catch (e) {
        console.log('Play toggle error:', e);
      }
    }

    // Pure sequential navigation through the playlist from here on — the
    // only randomization happens once, in startAtRandomIndex() above, when
    // the page first loads.
    function nextTrack() {
      if (!ytPlayer || typeof ytPlayer.nextVideo !== 'function') return;
      try {
        ytPlayer.nextVideo();
        setTimeout(updateTrackData, 300);
      } catch (e) {}
    }

    function prevTrack() {
      if (!ytPlayer || typeof ytPlayer.previousVideo !== 'function') return;
      try {
        ytPlayer.previousVideo();
        setTimeout(updateTrackData, 300);
      } catch (e) {}
    }

    const VOLUME_ICON_ON = '<path d="M11 5 6 9H2v6h4l5 4V5Z"></path><path d="M15.5 8.5a5 5 0 0 1 0 7"></path><path d="M18.5 5.5a9 9 0 0 1 0 13"></path>';
    const VOLUME_ICON_MUTED = '<path d="M11 5 6 9H2v6h4l5 4V5Z"></path><path d="m22 9-6 6"></path><path d="m16 9 6 6"></path>';

    function updateVolumeIcon(level) {
      const icon = document.getElementById('volumeIcon');
      const btn = document.getElementById('volumeButton');
      if (!icon) return;
      if (level > 0) {
        icon.innerHTML = VOLUME_ICON_ON;
        if (btn) btn.setAttribute('aria-label', 'Volume: ' + level + '%');
      } else {
        icon.innerHTML = VOLUME_ICON_MUTED;
        if (btn) btn.setAttribute('aria-label', 'Muted');
      }
    }

    function toggleVolumePopover(e) {
      e?.stopPropagation();
      const popover = document.getElementById('volumePopover');
      if (!popover) return;
      popover.classList.toggle('hidden');
    }

    function setPlayerVolume(level) {
      if (!ytPlayer) return;
      try {
        level = Math.max(0, Math.min(100, level));
        if (level === 0) {
          ytPlayer.mute();
        } else {
          ytPlayer.unMute();
          ytPlayer.setVolume(level);
        }
        updateVolumeIcon(level);
      } catch (e) {}
    }

    document.addEventListener('DOMContentLoaded', () => {
      document.getElementById('coverButton')?.addEventListener('click', togglePlay);
      document.getElementById('playButton')?.addEventListener('click', togglePlay);
      document.getElementById('prevButton')?.addEventListener('click', prevTrack);
      document.getElementById('nextButton')?.addEventListener('click', nextTrack);

      const volumeBtn = document.getElementById('volumeButton');
      const volumePopover = document.getElementById('volumePopover');
      const volumeSlider = document.getElementById('volumeSlider');

      volumeBtn?.addEventListener('click', toggleVolumePopover);
      volumeSlider?.addEventListener('input', (e) => {
        setPlayerVolume(parseInt(e.target.value, 10));
      });
      volumePopover?.addEventListener('click', (e) => e.stopPropagation());
      document.addEventListener('click', () => {
        volumePopover?.classList.add('hidden');
      });

      const progressBar = document.getElementById('progressBar');
      if (progressBar) {
        progressBar.addEventListener('click', (e) => {
          if (!ytPlayer || !ytPlayer.getDuration) return;
          const rect = progressBar.getBoundingClientRect();
          const pct = Math.max(0, Math.min(1, (e.clientX - rect.left) / rect.width));
          ytPlayer.seekTo(pct * ytPlayer.getDuration(), true);
        });
      }
    });

    // =========================================================================
    // REALISTIC RAIN, LIGHTNING & THUNDER ENGINE (Canvas + Web Audio API)
    // =========================================================================
    (function () {
      const toggleBtn = document.getElementById('rainThunderToggleBtn');
      const canvas = document.getElementById('rainCanvas');
      const flashOverlay = document.getElementById('lightningFlashOverlay');
      if (!toggleBtn || !canvas || !flashOverlay) return;

      const ctx = canvas.getContext('2d');
      let isRainActive = false;
      let animFrameId = null;
      let lightningTimeout = null;
      let drops = [];
      let splashes = [];
      let width = window.innerWidth;
      let height = window.innerHeight;

      let audioCtx = null;
      let rainGainNode = null;
      let rainNoiseSource = null;

      function resizeCanvas() {
        width = canvas.width = window.innerWidth;
        height = canvas.height = window.innerHeight;
      }
      window.addEventListener('resize', resizeCanvas);
      resizeCanvas();

      class Drop {
        constructor() {
          this.reset(true);
        }
        reset(randomY) {
          this.x = Math.random() * (width + 200) - 100;
          this.y = randomY ? Math.random() * height : -20 - Math.random() * 50;
          this.z = Math.random() * 0.8 + 0.2;
          this.speed = (18 + Math.random() * 10) * this.z;
          this.len = (15 + Math.random() * 15) * this.z;
          this.wind = -2.5 * this.z;
          this.alpha = 0.2 + this.z * 0.45;
          this.thickness = 0.8 + this.z * 1.1;
        }
        update() {
          this.x += this.wind;
          this.y += this.speed;
          if (this.y > height - 30) {
            if (Math.random() < 0.35 && splashes.length < 120) {
              splashes.push(new Splash(this.x, height - 10, this.z));
            }
            this.reset(false);
          }
        }
        draw() {
          ctx.beginPath();
          ctx.moveTo(this.x, this.y);
          ctx.lineTo(this.x + this.wind * 1.5, this.y + this.len);
          ctx.strokeStyle = `rgba(180, 215, 255, ${this.alpha})`;
          ctx.lineWidth = this.thickness;
          ctx.stroke();
        }
      }

      class Splash {
        constructor(x, y, z) {
          this.x = x;
          this.y = y;
          this.z = z;
          this.vx = (Math.random() - 0.5) * 4 * z;
          this.vy = -(1.5 + Math.random() * 3) * z;
          this.gravity = 0.25;
          this.life = 1;
          this.decay = 0.08 + Math.random() * 0.06;
          this.radius = (1.2 + Math.random() * 1.5) * z;
        }
        update() {
          this.x += this.vx;
          this.y += this.vy;
          this.vy += this.gravity;
          this.life -= this.decay;
        }
        draw() {
          if (this.life <= 0) return;
          ctx.beginPath();
          ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
          ctx.fillStyle = `rgba(195, 225, 255, ${this.life * 0.5})`;
          ctx.fill();
        }
      }

      function initDrops() {
        const dropCount = Math.min(300, Math.floor(width * 0.25));
        drops = [];
        for (let i = 0; i < dropCount; i++) {
          drops.push(new Drop());
        }
      }

      function drawLightningBolt(startX, startY, endX, endY) {
        ctx.save();
        ctx.strokeStyle = 'rgba(240, 248, 255, 0.95)';
        ctx.lineWidth = 3.5;
        ctx.shadowColor = 'rgba(147, 197, 253, 1)';
        ctx.shadowBlur = 18;

        ctx.beginPath();
        let curX = startX;
        let curY = startY;
        ctx.moveTo(curX, curY);

        const steps = 14 + Math.floor(Math.random() * 8);
        for (let i = 0; i < steps; i++) {
          const t = (i + 1) / steps;
          const targetX = startX + (endX - startX) * t + (Math.random() - 0.5) * 70;
          const targetY = startY + (endY - startY) * t;
          ctx.lineTo(targetX, targetY);

          if (Math.random() < 0.4 && i < steps - 2) {
            ctx.moveTo(targetX, targetY);
            ctx.lineTo(targetX + (Math.random() - 0.5) * 90, targetY + 30 + Math.random() * 40);
            ctx.moveTo(targetX, targetY);
          }
        }
        ctx.stroke();
        ctx.restore();
      }

      function triggerLightning() {
        if (!isRainActive) return;

        const flashIntensity = 0.65 + Math.random() * 0.35;
        flashOverlay.style.opacity = flashIntensity.toString();

        const boltStartX = Math.random() * width * 0.8 + width * 0.1;
        const boltEndX = boltStartX + (Math.random() - 0.5) * 200;
        drawLightningBolt(boltStartX, 0, boltEndX, height * 0.7);

        setTimeout(() => {
          if (!isRainActive) return;
          flashOverlay.style.opacity = '0.15';
          setTimeout(() => {
            if (!isRainActive) return;
            flashOverlay.style.opacity = (flashIntensity * 0.8).toString();
            setTimeout(() => {
              flashOverlay.style.opacity = '0';
            }, 60);
          }, 45);
        }, 50);

        playThunderSound();

        const nextDelay = 5000 + Math.random() * 9000;
        lightningTimeout = setTimeout(triggerLightning, nextDelay);
      }

      function initAudio() {
        if (audioCtx) return;
        try {
          const AudioContext = window.AudioContext || window.webkitAudioContext;
          if (!AudioContext) return;
          audioCtx = new AudioContext();

          const bufferSize = audioCtx.sampleRate * 2;
          const noiseBuffer = audioCtx.createBuffer(1, bufferSize, audioCtx.sampleRate);
          const output = noiseBuffer.getChannelData(0);
          let lastOut = 0.0;
          for (let i = 0; i < bufferSize; i++) {
            const white = Math.random() * 2 - 1;
            output[i] = (lastOut + 0.02 * white) / 1.02;
            lastOut = output[i];
            output[i] *= 2.8;
          }

          rainNoiseSource = audioCtx.createBufferSource();
          rainNoiseSource.buffer = noiseBuffer;
          rainNoiseSource.loop = true;

          const filter = audioCtx.createBiquadFilter();
          filter.type = 'lowpass';
          filter.frequency.value = 1000;

          rainGainNode = audioCtx.createGain();
          rainGainNode.gain.setValueAtTime(0.001, audioCtx.currentTime);

          rainNoiseSource.connect(filter);
          filter.connect(rainGainNode);
          rainGainNode.connect(audioCtx.destination);
          rainNoiseSource.start(0);
        } catch (e) {
          console.warn('AudioContext rain init notice:', e);
        }
      }

      function playThunderSound() {
        if (!audioCtx) return;
        try {
          if (audioCtx.state === 'suspended') {
            audioCtx.resume();
          }
          const now = audioCtx.currentTime;

          const osc = audioCtx.createOscillator();
          const oscGain = audioCtx.createGain();
          osc.type = 'sawtooth';
          osc.frequency.setValueAtTime(80, now);
          osc.frequency.exponentialRampToValueAtTime(25, now + 2.5);

          const filter = audioCtx.createBiquadFilter();
          filter.type = 'lowpass';
          filter.frequency.setValueAtTime(160, now);
          filter.frequency.linearRampToValueAtTime(60, now + 2.5);

          oscGain.gain.setValueAtTime(0.01, now);
          oscGain.gain.linearRampToValueAtTime(0.22, now + 0.1);
          oscGain.gain.exponentialRampToValueAtTime(0.001, now + 3.0);

          osc.connect(filter);
          filter.connect(oscGain);
          oscGain.connect(audioCtx.destination);

          osc.start(now);
          osc.stop(now + 3.2);

          const crackBuffer = audioCtx.createBuffer(1, audioCtx.sampleRate * 2.2, audioCtx.sampleRate);
          const data = crackBuffer.getChannelData(0);
          for (let i = 0; i < data.length; i++) {
            data[i] = (Math.random() * 2 - 1) * Math.exp(-i / (audioCtx.sampleRate * 0.6));
          }
          const crackSource = audioCtx.createBufferSource();
          crackSource.buffer = crackBuffer;

          const crackFilter = audioCtx.createBiquadFilter();
          crackFilter.type = 'bandpass';
          crackFilter.frequency.setValueAtTime(320, now);
          crackFilter.Q.value = 1.8;

          const crackGain = audioCtx.createGain();
          crackGain.gain.setValueAtTime(0.18, now);
          crackGain.gain.exponentialRampToValueAtTime(0.001, now + 2.2);

          crackSource.connect(crackFilter);
          crackFilter.connect(crackGain);
          crackGain.connect(audioCtx.destination);

          crackSource.start(now + 0.05);
        } catch (e) {
          console.warn('Thunder sound trigger notice:', e);
        }
      }

      function animate() {
        if (!isRainActive) return;
        ctx.clearRect(0, 0, width, height);

        for (let i = 0; i < drops.length; i++) {
          drops[i].update();
          drops[i].draw();
        }

        for (let i = splashes.length - 1; i >= 0; i--) {
          splashes[i].update();
          splashes[i].draw();
          if (splashes[i].life <= 0) {
            splashes.splice(i, 1);
          }
        }

        animFrameId = requestAnimationFrame(animate);
      }

      function startRain() {
        isRainActive = true;
        initAudio();
        if (audioCtx && audioCtx.state === 'suspended') {
          audioCtx.resume();
        }
        if (rainGainNode && audioCtx) {
          rainGainNode.gain.cancelScheduledValues(audioCtx.currentTime);
          rainGainNode.gain.setValueAtTime(rainGainNode.gain.value, audioCtx.currentTime);
          rainGainNode.gain.linearRampToValueAtTime(0.12, audioCtx.currentTime + 1.2);
        }

        initDrops();
        canvas.classList.add('active');
        toggleBtn.classList.add('active');
        toggleBtn.setAttribute('aria-pressed', 'true');
        toggleBtn.innerHTML = `
          <span id="rainIcon" class="text-sm animate-bounce" aria-hidden="true">⚡</span>
          <span id="rainText" class="font-medium tracking-wide">Sirf Gaane</span>
          <span class="text-[10px] px-1.5 py-0.5 rounded-full bg-emerald-500/30 text-emerald-300 font-mono">ON</span>
        `;

        animate();

        if (lightningTimeout) clearTimeout(lightningTimeout);
        lightningTimeout = setTimeout(triggerLightning, 800);
      }

      function stopRain() {
        isRainActive = false;
        canvas.classList.remove('active');
        toggleBtn.classList.remove('active');
        toggleBtn.setAttribute('aria-pressed', 'false');
        toggleBtn.innerHTML = `
          <span id="rainIcon" class="text-sm transition-transform group-hover:scale-125" aria-hidden="true">🌧️</span>
          <span id="rainText" class="font-medium tracking-wide">Barish or Gaane? </span>
          <span class="text-[10px] px-1.5 py-0.5 rounded-full bg-blue-500/20 text-blue-300 font-mono group-hover:bg-blue-500/30">OFF</span>
        `;

        if (rainGainNode && audioCtx) {
          rainGainNode.gain.cancelScheduledValues(audioCtx.currentTime);
          rainGainNode.gain.setValueAtTime(rainGainNode.gain.value, audioCtx.currentTime);
          rainGainNode.gain.linearRampToValueAtTime(0.0001, audioCtx.currentTime + 0.8);
        }

        if (animFrameId) cancelAnimationFrame(animFrameId);
        if (lightningTimeout) clearTimeout(lightningTimeout);
        flashOverlay.style.opacity = '0';
        ctx.clearRect(0, 0, width, height);
      }

      toggleBtn.addEventListener('click', (e) => {
        e.preventDefault();
        if (isRainActive) {
          stopRain();
        } else {
          startRain();
        }
      });
    })();

    function getSynchronizedGlobalCount() {
      const nowSec = Math.floor(Date.now() / 1000);
      const interval5s = Math.floor(nowSec / 5);
      const date = new Date();
      const utcHours = date.getUTCHours() + date.getUTCMinutes() / 60;
      const dailyFactor = Math.sin(((utcHours - 10.5) / 24) * 2 * Math.PI);
      const baseCount = 1380 + Math.floor(dailyFactor * 300);
      function getNoise(tick) {
        const x = Math.sin(tick * 12.9898 + 78.233) * 43758.5453;
        const rand = x - Math.floor(x);
        return Math.floor((rand - 0.48) * 28);
      }
      return Math.max(980, baseCount + getNoise(interval5s));
    }
    function updateOnlineCounter() {
      const el = document.getElementById('onlineCount');
      if (el) el.innerText = getSynchronizedGlobalCount();
    }
    setInterval(updateOnlineCounter, 1000);
    updateOnlineCounter();
  </script>

  <!-- ============================== LIVE CHAT LOGIC ============================== -->
  <script>
    (function () {
      const toggleBtn = document.getElementById('liveChatToggleBtn');
      const badgeEl = document.getElementById('liveChatBadge');
      const panel = document.getElementById('liveChatPanel');
      const closeBtn = document.getElementById('liveChatCloseBtn');
      const messagesEl = document.getElementById('chatMessages');
      const errorBanner = document.getElementById('chatErrorBanner');
      const sendForm = document.getElementById('chatSendForm');
      const messageInput = document.getElementById('chatMessageInput');
      const sendBtn = document.getElementById('chatSendBtn');
      const namePrompt = document.getElementById('chatNamePrompt');
      const nameInput = document.getElementById('chatNameInput');
      const nameSaveBtn = document.getElementById('chatNameSaveBtn');
      const nameCancelBtn = document.getElementById('chatNameCancelBtn');
      const pendingPreview = document.getElementById('chatPendingPreview');
      if (!toggleBtn || !panel) return;

      const NAME_KEY = 'deluxe_chat_name';
      const FETCH_URL = '/chat/chat_fetch.php';
      const SEND_URL = '/chat/chat_send.php';
      const ACTIVE_POLL_MS = 1500;
      const BACKGROUND_POLL_MS = 8000;
      const SEND_COOLDOWN_MS = 3000;

      const NAME_COLORS = ['#f87171', '#fb923c', '#facc15', '#a3e635', '#34d399', '#22d3ee', '#60a5fa', '#a78bfa', '#f472b6', '#fb7185'];
      function colorForName(name) {
        let hash = 0;
        for (let i = 0; i < name.length; i++) hash = (hash * 31 + name.charCodeAt(i)) >>> 0;
        return NAME_COLORS[hash % NAME_COLORS.length];
      }

      let lastMessageId = 0;
      let activePollTimer = null;
      let backgroundPollTimer = null;
      let lastSendTime = 0;
      let pendingMessageText = null;
      let unreadCount = 0;
      let historyLoaded = false;
      let historyLoadPromise = null;
      const renderedIds = new Set();

      function formatMsgTime(iso) {
        try {
          // created_at is stored in UTC (see chat_config.php) — appending
          // "Z" tells the browser to treat it as UTC before converting,
          // then we force the display into IST regardless of the visitor's
          // own device timezone.
          const d = iso ? new Date(iso.replace(' ', 'T') + 'Z') : new Date();
          return d.toLocaleTimeString('en-IN', {
            timeZone: 'Asia/Kolkata',
            hour: 'numeric',
            minute: '2-digit',
            hour12: true
          });
        } catch (e) {
          return '';
        }
      }

      function showError(text) {
        if (!errorBanner) return;
        errorBanner.textContent = text;
        errorBanner.classList.remove('hidden');
      }

      function clearError() {
        if (!errorBanner) return;
        errorBanner.classList.add('hidden');
        errorBanner.textContent = '';
      }

      function isNearBottom() {
        return messagesEl.scrollHeight - messagesEl.scrollTop - messagesEl.clientHeight < 60;
      }

      function updateBadge() {
        if (!badgeEl) return;
        if (unreadCount > 0) {
          badgeEl.textContent = unreadCount > 9 ? '9+' : String(unreadCount);
          badgeEl.classList.add('show');
        } else {
          badgeEl.classList.remove('show');
        }
      }

      function appendMessage(msg) {
        if (msg.id != null) {
          if (renderedIds.has(msg.id)) return;
          renderedIds.add(msg.id);
        }

        const myName = localStorage.getItem(NAME_KEY);
        const isOwn = !!myName && msg.name === myName;
        const wasNearBottom = isNearBottom();

        const row = document.createElement('div');
        row.className = 'chat-msg-row ' + (isOwn ? 'own' : 'other');

        const bubble = document.createElement('div');
        bubble.className = 'chat-msg';

        if (!isOwn) {
          const nameEl = document.createElement('span');
          nameEl.className = 'chat-msg-name';
          nameEl.style.color = colorForName(msg.name);
          nameEl.textContent = msg.name;
          bubble.appendChild(nameEl);
        }

        const textEl = document.createElement('span');
        textEl.textContent = msg.message;
        bubble.appendChild(textEl);

        const timeEl = document.createElement('span');
        timeEl.className = 'chat-msg-time';
        timeEl.textContent = formatMsgTime(msg.created_at);
        bubble.appendChild(timeEl);

        row.appendChild(bubble);
        messagesEl.appendChild(row);
        if (wasNearBottom) {
          messagesEl.scrollTop = messagesEl.scrollHeight;
        }
      }

      async function loadInitialMessages() {
        try {
          const res = await fetch(`${FETCH_URL}?_=${Date.now()}`, { cache: 'no-store' });
          if (!res.ok) {
            let detail = '';
            try { detail = (await res.json()).error || ''; } catch (e2) {}
            showError(
              'Could not load chat messages (server responded ' + res.status + '). ' +
              (detail || 'Check that /chat/chat_config.php has the correct database credentials.')
            );
            return;
          }
          clearError();
          const data = await res.json();
          (data.messages || []).forEach((m) => {
            appendMessage(m);
            lastMessageId = Math.max(lastMessageId, parseInt(m.id, 10) || 0);
          });
          messagesEl.scrollTop = messagesEl.scrollHeight;
          historyLoaded = true;
        } catch (e) {
          showError('Could not reach the chat server. Check that chat_fetch.php was uploaded to /chat/ on your server.');
        }
      }

      function ensureHistoryLoaded() {
        if (!historyLoadPromise) {
          historyLoadPromise = loadInitialMessages();
        }
        return historyLoadPromise;
      }

      async function pollNewMessages(isBackground) {
        try {
          const res = await fetch(`${FETCH_URL}?after_id=${lastMessageId}&_=${Date.now()}`, { cache: 'no-store' });
          if (!res.ok) return;
          const data = await res.json();
          const newMsgs = data.messages || [];
          if (!newMsgs.length) return;
          clearError();

          const myName = localStorage.getItem(NAME_KEY);
          newMsgs.forEach((m) => {
            lastMessageId = Math.max(lastMessageId, parseInt(m.id, 10) || 0);
            if (isBackground) {
              if (!myName || m.name !== myName) unreadCount++;
            } else {
              appendMessage(m);
            }
          });
          if (isBackground) updateBadge();
        } catch (e) {
          // Silent on background/poll ticks.
        }
      }

      function startActivePolling() {
        stopBackgroundPolling();
        if (activePollTimer) return;
        activePollTimer = setInterval(() => pollNewMessages(false), ACTIVE_POLL_MS);
      }

      function stopActivePolling() {
        if (activePollTimer) {
          clearInterval(activePollTimer);
          activePollTimer = null;
        }
      }

      function startBackgroundPolling() {
        if (backgroundPollTimer || !historyLoaded) return;
        backgroundPollTimer = setInterval(() => pollNewMessages(true), BACKGROUND_POLL_MS);
      }

      function stopBackgroundPolling() {
        if (backgroundPollTimer) {
          clearInterval(backgroundPollTimer);
          backgroundPollTimer = null;
        }
      }

      function scrollToBottom() {
        requestAnimationFrame(() => {
          messagesEl.scrollTop = messagesEl.scrollHeight;
        });
      }

      function openPanel() {
        panel.classList.add('open');
        document.body.style.overflow = 'hidden';
        unreadCount = 0;
        updateBadge();
        scrollToBottom();
        ensureHistoryLoaded().then(() => {
          scrollToBottom();
          return pollNewMessages(false);
        }).then(() => {
          scrollToBottom();
          startActivePolling();
        });
        setTimeout(() => messageInput?.focus(), 150);
      }

      function closePanel() {
        panel.classList.remove('open');
        document.body.style.overflow = '';
        stopActivePolling();
        startBackgroundPolling();
      }

      toggleBtn.addEventListener('click', openPanel);
      closeBtn?.addEventListener('click', closePanel);
      panel.addEventListener('click', (e) => { if (e.target === panel) closePanel(); });
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && panel.classList.contains('open')) closePanel();
      });

      async function actuallySendMessage(name, text) {
        try {
          sendBtn.disabled = true;
          const res = await fetch(SEND_URL, {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ name, message: text })
          });
          const data = await res.json();
          if (!res.ok) {
            showError(data.error || 'Message could not be sent. Please try again.');
            sendBtn.disabled = false;
            return;
          }
          clearError();
          appendMessage({ id: data.id, name, message: text, created_at: null });
          lastMessageId = Math.max(lastMessageId, parseInt(data.id, 10) || 0);
        } catch (e) {
          showError('Could not reach the chat server. Check that chat_send.php was uploaded to /chat/ on your server.');
        } finally {
          setTimeout(() => { sendBtn.disabled = false; }, SEND_COOLDOWN_MS);
        }
      }

      // Blocks links/URLs client-side for instant feedback — the real
      // enforcement is server-side in chat_send.php.
      const LINK_PATTERN = /(https?:\/\/|www\.)\S+|\b[a-z0-9-]+\.(com|net|org|in|co|io|me|xyz|info|biz|link|shop|online|site|tv|app|ai|gg|ly|to)\b/i;

      function showNamePrompt(text) {
        pendingMessageText = text;
        if (pendingPreview) pendingPreview.textContent = '"' + text + '"';
        sendForm?.classList.add('hidden');
        namePrompt?.classList.remove('hidden');
        nameInput?.focus();
      }

      function hideNamePrompt() {
        namePrompt?.classList.add('hidden');
        sendForm?.classList.remove('hidden');
        pendingMessageText = null;
      }

      sendForm?.addEventListener('submit', (e) => {
        e.preventDefault();
        const text = (messageInput?.value || '').trim();
        if (!text) return;

        if (LINK_PATTERN.test(text)) {
          showError('Links aren\'t allowed in chat messages.');
          return;
        }

        const now = Date.now();
        if (now - lastSendTime < SEND_COOLDOWN_MS) return;

        const savedName = localStorage.getItem(NAME_KEY);
        if (!savedName) {
          messageInput.value = '';
          showNamePrompt(text);
          return;
        }

        lastSendTime = now;
        messageInput.value = '';
        actuallySendMessage(savedName, text);
      });

      nameSaveBtn?.addEventListener('click', () => {
        const name = (nameInput?.value || '').trim();
        if (!name) {
          nameInput?.focus();
          return;
        }
        localStorage.setItem(NAME_KEY, name);
        const text = pendingMessageText;
        hideNamePrompt();
        if (text) {
          lastSendTime = Date.now();
          actuallySendMessage(name, text);
        }
        messageInput?.focus();
      });

      nameCancelBtn?.addEventListener('click', () => {
        hideNamePrompt();
        messageInput?.focus();
      });

      nameInput?.addEventListener('keydown', (e) => {
        if (e.key === 'Enter') {
          e.preventDefault();
          nameSaveBtn?.click();
        }
      });

      ensureHistoryLoaded().then(() => {
        if (!panel.classList.contains('open')) {
          startBackgroundPolling();
        }
      });
    })();
  </script>
</body>
</html>
